---
phase: 07-registry-and-routing-observability
plan: "02"
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - tests/safemoe/test_evaluate.py
  - tests/safemoe/test_pretrain.py
  - safemoe/observability.py
  - safemoe/evaluate.py
  - safemoe/pretrain.py
autonomous: true
requirements:
  - ROUT-02
  - ROUT-03
must_haves:
  truths:
    - "Baseline, warmup, transfer, and ablation evaluation can all emit the same routing observability schema through one shared codepath."
    - "Routing artifacts include raw dispatch counts and harmful-routing fractions for each available split, with absent splits omitted instead of populated with placeholder values."
    - "A parity-checking flow compares logged routing metrics against actual dispatched counts and raises a hard failure on any mismatch."
  artifacts:
    - path: "tests/safemoe/test_evaluate.py"
      provides: "Coverage for shared routing artifact generation in the evaluation path."
      min_lines: 220
    - path: "tests/safemoe/test_pretrain.py"
      provides: "Coverage for checkpoint-local routing artifact generation and parity failure handling in the training path."
      min_lines: 820
    - path: "safemoe/observability.py"
      provides: "Shared routing collector, artifact writer, and parity helper reused by train and eval."
      exports: ["RoutingObservabilityCollector", "write_routing_artifacts", "assert_routing_parity"]
  key_links:
    - from: "safemoe/evaluate.py"
      to: "safemoe/observability.py"
      via: "eval routing attribution becomes a compatibility wrapper around the shared collector"
      pattern: "RoutingObservabilityCollector|write_routing_artifacts"
    - from: "safemoe/pretrain.py"
      to: "safemoe/observability.py"
      via: "save/eval path writes stage-local routing artifacts and can trigger parity checks"
      pattern: "assert_routing_parity|routing_observability"
---

<objective>
Create one shared routing observability path for evaluation and training, including a hard-fail parity check between logged routing metrics and real dispatched expert behavior.

Purpose: Close `ROUT-02` and `ROUT-03` without leaving eval and training on divergent routing schemas.
Output: Shared routing artifacts for train/eval plus an explicit parity-checking flow.
</objective>

<execution_context>
@/home/jincheng1/.codex/get-shit-done/workflows/execute-plan.md
@/home/jincheng1/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-registry-and-routing-observability/07-CONTEXT.md
@.planning/phases/07-registry-and-routing-observability/07-RESEARCH.md
@.planning/phases/07-registry-and-routing-observability/07-VALIDATION.md
@.planning/phases/07-registry-and-routing-observability/07-01-PLAN.md
@safemoe/model.py
@safemoe/evaluate.py
@safemoe/pretrain.py
@tests/safemoe/test_evaluate.py
@tests/safemoe/test_pretrain.py

<interfaces>
From `safemoe/model.py`:
```python
class SafeMoELayer(LLaMAMoE):
    def forward(self, x: torch.Tensor) -> torch.Tensor:
        ...
        self._last_indices = indices
```

Current eval metric keys:
```text
routing_harmful_frac_D_std
routing_harmful_frac_D_harmful
```

Required artifact contract:
```text
routing_observability.json
routing_observability.md
routing_parity.json   # parity flows only
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED coverage for the shared routing artifact contract</name>
  <files>tests/safemoe/test_evaluate.py, tests/safemoe/test_pretrain.py</files>
  <read_first>.planning/phases/07-registry-and-routing-observability/07-CONTEXT.md, .planning/phases/07-registry-and-routing-observability/07-RESEARCH.md, .planning/phases/07-registry-and-routing-observability/07-VALIDATION.md, safemoe/evaluate.py, safemoe/pretrain.py, safemoe/model.py, tests/safemoe/test_registry.py</read_first>
  <behavior>
    - Test 1: `routing_attribution()` continues to return `routing_harmful_frac_D_std` and `routing_harmful_frac_D_harmful`, but now also writes `routing_observability.json` and `routing_observability.md` with raw dispatch counts.
    - Test 2: The shared artifact omits any `D_unlabeled` key when that split is unavailable.
    - Test 3: A parity-checking training or checkpoint-local flow writes `routing_parity.json` and raises `ValueError` when logged routing metrics disagree with collector-derived counts.
  </behavior>
  <action>Extend `tests/safemoe/test_evaluate.py` with `test_routing_observability_writes_shared_artifacts` that asserts exact artifact names `routing_observability.json` and `routing_observability.md`, checks for keys `routing_harmful_frac_D_std`, `routing_harmful_frac_D_harmful`, `dispatch_count_D_std`, and `dispatch_count_D_harmful`, and confirms no `D_unlabeled` routing keys appear when the mock datamodule lacks that split. Extend `tests/safemoe/test_pretrain.py` with `test_routing_parity_fails_on_mismatch` that feeds intentionally inconsistent logged routing metrics into the parity helper, asserts `ValueError` is raised, and asserts a `routing_parity.json` artifact is written with a failing result.</action>
  <verify>
    <automated>pytest tests/safemoe/test_evaluate.py -k "routing" -x && pytest tests/safemoe/test_pretrain.py -k "routing parity" -x</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "routing_observability.json|routing_observability.md|routing_parity.json|dispatch_count_D_std|dispatch_count_D_harmful" tests/safemoe/test_evaluate.py tests/safemoe/test_pretrain.py` finds the exact artifact and metric strings in the tests.
    - `rg -n "D_unlabeled" tests/safemoe/test_evaluate.py` confirms the test explicitly checks omission behavior.
    - `pytest tests/safemoe/test_evaluate.py -k "routing" -x` exits 0.
    - `pytest tests/safemoe/test_pretrain.py -k "routing parity" -x` exits 0.
  </acceptance_criteria>
  <done>Phase 7 has locked tests for one shared routing schema and the parity hard-failure contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the shared collector and wire it into eval and pretrain</name>
  <files>tests/safemoe/test_evaluate.py, tests/safemoe/test_pretrain.py, safemoe/observability.py, safemoe/evaluate.py, safemoe/pretrain.py</files>
  <read_first>tests/safemoe/test_evaluate.py, tests/safemoe/test_pretrain.py, safemoe/model.py, safemoe/evaluate.py, safemoe/pretrain.py, .planning/phases/04-ablation-evaluation/04-CONTEXT.md</read_first>
  <behavior>
    - Test 1: The shared collector uses `SafeMoELayer._last_indices` via forward hooks, aggregates raw dispatch counts per split, and computes exact harmful-routing fractions from those counts.
    - Test 2: `routing_attribution()` becomes a compatibility wrapper that still returns the existing fraction keys while delegating artifact writing to the shared collector.
    - Test 3: `pretrain.py` can write the same routing artifact schema beside checkpoint or stage output directories and optionally run a parity check that raises on mismatch.
  </behavior>
  <action>Create `safemoe/observability.py` with `RoutingObservabilityCollector`, `write_routing_artifacts(output_dir: Path, metrics: dict, markdown_title: str)`, and `assert_routing_parity(logged_metrics: dict, observed_metrics: dict, output_dir: Path)`. The collector should register forward hooks on every `SafeMoELayer`, read `module._last_indices`, accumulate exact integer counts per split, and emit metric keys `dispatch_count_<split>` and `routing_harmful_frac_<split>`. In `assert_routing_parity`, compare logged metrics against observed metrics split by split, write `output_dir / "routing_parity.json"` with keys `ok`, `checks`, and `mismatches`, and raise `ValueError("Routing parity check failed")` on any mismatch. Refactor `safemoe/evaluate.py::routing_attribution()` to use the shared collector, keep its return signature backward-compatible, and write artifacts exactly to `routing_observability.json` and `routing_observability.md`. In `safemoe/pretrain.py`, add a checkpoint-local routing observability call in the save/eval path so stage outputs can write the same artifacts beside the checkpoint directory; if a parity-checking path is enabled for that flow, call `assert_routing_parity(...)` on the same shared metrics instead of recomputing routing through a second implementation. Omit unavailable splits entirely rather than writing placeholder values.</action>
  <verify>
    <automated>pytest tests/safemoe/test_evaluate.py -k "routing" -x && pytest tests/safemoe/test_pretrain.py -k "routing parity" -x</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "class RoutingObservabilityCollector|write_routing_artifacts|assert_routing_parity" safemoe/observability.py` finds the exact shared helper names.
    - `rg -n "routing_observability.json|routing_observability.md|routing_parity.json" safemoe/observability.py safemoe/evaluate.py safemoe/pretrain.py` finds the exact artifact names.
    - `rg -n "dispatch_count_D_std|dispatch_count_D_harmful|routing_harmful_frac_D_std|routing_harmful_frac_D_harmful" safemoe/observability.py safemoe/evaluate.py tests/safemoe/test_evaluate.py` finds the shared metric contract.
    - `rg -n "Routing parity check failed" safemoe/observability.py safemoe/pretrain.py tests/safemoe/test_pretrain.py` finds the exact hard-failure contract.
    - `pytest tests/safemoe/test_evaluate.py -k "routing" -x` exits 0.
    - `pytest tests/safemoe/test_pretrain.py -k "routing parity" -x` exits 0.
  </acceptance_criteria>
  <done>Train and eval now share one routing observability path and one parity contract, so Phase 8-10 can reuse the same artifact schema.</done>
</task>

</tasks>

<verification>
Run `pytest tests/safemoe/test_evaluate.py -k "routing" -x` and `pytest tests/safemoe/test_pretrain.py -k "routing parity" -x` after the plan completes, then run the full Phase 7 suite `pytest tests/safemoe/test_registry.py tests/safemoe/test_evaluate.py tests/safemoe/test_pretrain.py -x`. Confirm the written artifacts contain raw dispatch counts and harmful-routing fractions only for splits that were actually observed, and that the parity helper raises on mismatches.
</verification>

<success_criteria>
`ROUT-02` and `ROUT-03` are satisfied: researchers can collect comparable routing metrics through one shared path across train/eval flows, and a parity-checking flow proves the logged routing signal exactly matches the real dispatched harmful-expert behavior or fails hard.
</success_criteria>

<output>
After completion, create `.planning/phases/07-registry-and-routing-observability/07-02-SUMMARY.md`
</output>
