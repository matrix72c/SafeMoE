---
phase: 07-registry-and-routing-observability
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/safemoe/test_registry.py
  - safemoe/masking.py
autonomous: true
requirements:
  - ROUT-01
must_haves:
  truths:
    - "Researcher can inspect exhaustive, non-overlapping ownership of every post-intervention parameter through explicit `theta_harmful`, `theta_std`, and `theta_shared` inventory artifacts."
    - "Fused `attn.qkv.weight` harmful/std ownership is visible as first-class slice records without breaking the existing non-overlapping full-parameter registry contract."
    - "Router/gate weights remain classified as `theta_shared`, while any Phase 6 router-column provenance is exposed only as artifact annotations."
  artifacts:
    - path: "tests/safemoe/test_registry.py"
      provides: "Coverage for exhaustive registry inventory, `qkv` slice visibility, and JSON/Markdown artifact writing."
      min_lines: 120
    - path: "safemoe/masking.py"
      provides: "Artifact-grade registry inventory, grouped summary generation, and writer helpers built on top of `HarmfulParamRegistry`."
      exports: ["HarmfulParamRegistry", "write_registry_reports"]
  key_links:
    - from: "safemoe/masking.py"
      to: "safemoe/interventions/manifest.py"
      via: "registry summary may annotate derived router-column provenance without changing ownership classification"
      pattern: "derived_router_column_pairs|manifest"
---

<objective>
Extend the existing parameter registry into a researcher-facing ownership report that fully covers the direct-Qwen post-surgery checkpoint.

Purpose: Satisfy `ROUT-01` without changing Phase 2 masking semantics.
Output: `registry_inventory.json` plus `registry_summary.md`, with first-class `qkv` slice visibility and preserved `theta_shared` router semantics.
</objective>

<execution_context>
@/home/jincheng1/.codex/get-shit-done/workflows/execute-plan.md
@/home/jincheng1/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-registry-and-routing-observability/07-CONTEXT.md
@.planning/phases/07-registry-and-routing-observability/07-RESEARCH.md
@.planning/phases/07-registry-and-routing-observability/07-VALIDATION.md
@.planning/phases/06-checkpoint-surgery/06-CONTEXT.md
@safemoe/masking.py
@safemoe/interventions/manifest.py
@tests/safemoe/test_registry.py

<interfaces>
From `safemoe/masking.py`:
```python
class HarmfulParamRegistry:
    def parameters_by_type(self, split: str) -> list[nn.Parameter]: ...
```

Phase 7 artifact contract:
```text
registry_inventory.json
registry_summary.md
```

Required inventory concepts:
```text
ownership: theta_harmful | theta_std | theta_shared
category: expert | router_gate | embedding | attn_qkv_full | attn_qkv_slice | other
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED coverage for registry inventory and first-class qkv slice visibility</name>
  <files>tests/safemoe/test_registry.py</files>
  <read_first>.planning/phases/07-registry-and-routing-observability/07-CONTEXT.md, .planning/phases/07-registry-and-routing-observability/07-RESEARCH.md, .planning/phases/07-registry-and-routing-observability/07-VALIDATION.md, safemoe/masking.py, safemoe/interventions/manifest.py, tests/safemoe/test_evaluate.py</read_first>
  <behavior>
    - Test 1: `registry_inventory()` covers every `model.named_parameters()` entry exactly once by full parameter name and reports `ownership` values only from `theta_harmful`, `theta_std`, and `theta_shared`.
    - Test 2: When `harmful_attn_heads=[0, 1]`, the inventory contains explicit `attn_qkv_slice` records with `slice_role` equal to `harmful` or `std` and concrete `slice_rows`.
    - Test 3: `write_registry_reports()` writes both `registry_inventory.json` and `registry_summary.md`.
    - Test 4: Router/gate parameters appear with `ownership == "theta_shared"` even when manifest-derived provenance annotations are present.
  </behavior>
  <action>Extend `tests/safemoe/test_registry.py` with concrete artifact tests. Add `test_registry_inventory_is_exhaustive_and_named` that asserts every parameter name from `model.named_parameters()` appears exactly once in the inventory and no inventory row uses an ownership value outside `theta_harmful`, `theta_std`, or `theta_shared`. Add `test_registry_inventory_exposes_qkv_slice_rows` that builds `SafeMoEConfig` with `harmful_attn_heads=[0, 1]` and checks for `category == "attn_qkv_slice"`, `slice_role == "harmful"`, and non-empty `slice_rows`. Add `test_write_registry_reports_writes_json_and_markdown` that writes reports into a temp directory and asserts exact filenames `registry_inventory.json` and `registry_summary.md`. Add `test_router_gate_stays_theta_shared_in_inventory` that checks `transformer.h.0.mlp.gate.weight` is reported with `ownership == "theta_shared"`.</action>
  <verify>
    <automated>pytest tests/safemoe/test_registry.py -k "inventory or qkv or summary" -x</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "registry_inventory|registry_summary|attn_qkv_slice|theta_shared" tests/safemoe/test_registry.py` finds the exact artifact and category strings in the tests.
    - `pytest tests/safemoe/test_registry.py -k "inventory or qkv or summary" -x` exits 0.
  </acceptance_criteria>
  <done>Phase 7 has failing-then-passing tests that lock the researcher-facing registry artifact contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement artifact-grade registry inventory and summary writers</name>
  <files>tests/safemoe/test_registry.py, safemoe/masking.py</files>
  <read_first>tests/safemoe/test_registry.py, safemoe/masking.py, safemoe/interventions/manifest.py, .planning/phases/06-checkpoint-surgery/06-CONTEXT.md</read_first>
  <behavior>
    - Test 1: `HarmfulParamRegistry.registry_inventory()` returns one full-parameter record per named parameter plus additional `attn_qkv_slice` records when harmful heads are configured.
    - Test 2: `write_registry_reports(output_dir, manifest=None)` writes `registry_inventory.json` and `registry_summary.md` with grouped ownership counts and category counts.
    - Test 3: Gate/router parameters remain owned by `theta_shared`, but the summary can optionally include manifest-derived router-column pairs under a provenance note rather than changing ownership.
  </behavior>
  <action>Implement `registry_inventory()` and `write_registry_reports(output_dir: Path, manifest=None)` in `safemoe/masking.py`. Build each full-parameter record with exact keys `parameter_name`, `ownership`, `category`, and `shape`. Classify names matching `.mlp.experts.` as `category == "expert"`, names ending in `.mlp.gate.weight` as `category == "router_gate"`, names ending in `attn.qkv.weight` as `category == "attn_qkv_full"`, and embeddings such as `transformer.wte.weight` as `category == "embedding"`. When `self._qkv_harmful_metadata` or `self._qkv_std_metadata` are present, append extra records with `category == "attn_qkv_slice"`, `slice_role` equal to `harmful` or `std`, and `slice_rows` as `[start, stop]` pairs. Write the machine-readable artifact exactly to `output_dir / "registry_inventory.json"` and the human-readable artifact exactly to `output_dir / "registry_summary.md"`. In the Markdown summary, include sections headed `# Registry Summary`, `## Ownership Counts`, and `## Category Counts`. If `manifest` is provided, optionally add a short provenance section that lists `derived_router_column_pairs(manifest)` without changing any router record ownership to `theta_harmful`.</action>
  <verify>
    <automated>pytest tests/safemoe/test_registry.py -k "inventory or qkv or summary" -x</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "registry_inventory\\(|write_registry_reports|registry_inventory.json|registry_summary.md" safemoe/masking.py` finds the exact helper names and artifact names.
    - `rg -n "# Registry Summary|## Ownership Counts|## Category Counts" safemoe/masking.py` finds the required summary headings.
    - `rg -n "attn_qkv_slice|slice_role|slice_rows" safemoe/masking.py` finds explicit first-class qkv slice reporting.
    - `rg -n "theta_harmful.*gate.weight|theta_std.*gate.weight" safemoe/masking.py tests/safemoe/test_registry.py` returns no matches.
    - `pytest tests/safemoe/test_registry.py -k "inventory or qkv or summary" -x` exits 0.
  </acceptance_criteria>
  <done>Researchers can inspect complete registry ownership with explicit qkv slice visibility and preserved shared-router semantics.</done>
</task>

</tasks>

<verification>
Run `pytest tests/safemoe/test_registry.py -x` after the plan completes. Confirm the JSON artifact contains one full-parameter record for every named parameter, optional `attn_qkv_slice` records with explicit `slice_rows`, and no router/gate record classified outside `theta_shared`.
</verification>

<success_criteria>
`ROUT-01` is satisfied: a researcher can inspect exhaustive, non-overlapping parameter ownership for the post-intervention checkpoint through machine-readable and human-readable artifacts, with first-class qkv slice visibility and unchanged shared-router semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/07-registry-and-routing-observability/07-01-SUMMARY.md`
</output>
