---
phase: 02-model-architecture-masking
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/safemoe/test_config.py
  - tests/safemoe/test_model.py
  - tests/safemoe/test_registry.py
  - tests/safemoe/test_masking.py
  - safemoe/configs/safemoe-tinystories.yaml
autonomous: true
requirements: [MOE-01, MOE-02, MOE-03, MOE-04, MASK-01, MASK-02, MASK-03, MASK-04]

must_haves:
  truths:
    - "All four test files exist and are importable by pytest"
    - "Every test in the four files fails with ImportError or pytest.raises(NotImplementedError) — not passing by accident"
    - "safemoe-tinystories.yaml is a valid YAML with mlp_class_name: LLaMAMoE, n_expert: 8, harmful_expert_indices: [0, 1]"
    - "pytest tests/safemoe/test_config.py -x reports collected N items, all failing (RED)"
    - "pytest tests/safemoe/test_model.py -x reports collected N items, all failing (RED)"
    - "pytest tests/safemoe/test_registry.py -x reports collected N items, all failing (RED)"
    - "pytest tests/safemoe/test_masking.py -x reports collected N items, all failing (RED)"
  artifacts:
    - path: "tests/safemoe/test_config.py"
      provides: "Failing test stubs for MOE-01 (SafeMoEConfig)"
    - path: "tests/safemoe/test_model.py"
      provides: "Failing test stubs for MOE-03 and MOE-04 (SafeMoELayer structure + init)"
    - path: "tests/safemoe/test_registry.py"
      provides: "Failing test stubs for MOE-02 and MASK-03 (HarmfulParamRegistry)"
    - path: "tests/safemoe/test_masking.py"
      provides: "Failing test stubs for MASK-01, MASK-02, MASK-04 (maskers)"
    - path: "safemoe/configs/safemoe-tinystories.yaml"
      provides: "Experiment config for a small TinyStories MoE model"
  key_links:
    - from: "tests/safemoe/test_config.py"
      to: "safemoe.config.SafeMoEConfig"
      via: "import statement at top of test file"
    - from: "tests/safemoe/test_model.py"
      to: "safemoe.model.SafeMoELayer"
      via: "import statement at top of test file"
    - from: "tests/safemoe/test_registry.py"
      to: "safemoe.masking.HarmfulParamRegistry"
      via: "import statement at top of test file"
    - from: "tests/safemoe/test_masking.py"
      to: "safemoe.masking.GradientMasker, safemoe.masking.ActivationMasker"
      via: "import statement at top of test file"
---

<objective>
Create failing test stubs (RED state) for all Phase 2 requirements, plus the experiment YAML config.

Purpose: Establish the complete test contract for SafeMoEConfig, SafeMoELayer, HarmfulParamRegistry, GradientMasker, and ActivationMasker before any implementation. GREEN plans (02, 03, 04) implement against these stubs.

Output: Four test files in tests/safemoe/ (all failing RED), one YAML config in safemoe/configs/.
</objective>

<execution_context>
@/home/jincheng1/.claude/get-shit-done/workflows/execute-plan.md
@/home/jincheng1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-model-architecture-masking/02-CONTEXT.md
@.planning/phases/02-model-architecture-masking/02-RESEARCH.md
@.planning/phases/02-model-architecture-masking/02-VALIDATION.md
</context>

<interfaces>
<!-- Imports the test files will use — modules do NOT exist yet; tests fail on ImportError (RED). -->

Expected imports (will fail until implementation plans run):
```python
from safemoe.config import SafeMoEConfig
from safemoe.model import SafeMoELayer
from safemoe.masking import HarmfulParamRegistry, GradientMasker, ActivationMasker
```

Test config dimensions for all tests (CPU-only, < 5 seconds):
```python
SMALL_CONFIG = dict(
    padded_vocab_size=1024,
    vocab_size=1024,
    n_layer=2,
    n_embd=32,
    n_head=4,
    n_query_groups=4,
    head_size=8,
    n_expert=4,
    n_expert_per_token=2,
    moe_intermediate_size=64,
    mlp_class_name="LLaMAMoE",
    harmful_expert_indices=[0, 1],
    num_harmful_experts=2,
    harmful_attn_heads=[],
)
```

LLaMAMoE dispatch loop (from litgpt/model.py:793-819):
```python
def forward(self, x):
    # accumulates y[token_idx] += probs * expert(x[token_idx])
    # SafeMoELayer overrides to skip harmful experts when _activation_masking_enabled
```
</interfaces>

<tasks>

<task type="tdd">
  <name>Task 1: Write RED test stubs for SafeMoEConfig (test_config.py)</name>
  <files>tests/safemoe/test_config.py</files>
  <behavior>
    - test_safemoe_config_has_harmful_fields: SafeMoEConfig(harmful_expert_indices=[0,1], num_harmful_experts=2, harmful_attn_heads=[]) has those attributes with the given values
    - test_safemoe_config_defaults_safe: SafeMoEConfig() with no harmful args has harmful_expert_indices=[], num_harmful_experts=0, harmful_attn_heads=[]
    - test_safemoe_config_inherits_litgpt_config: isinstance(SafeMoEConfig(), litgpt.Config) is True
    - test_safemoe_config_mlp_class_returns_safemoe_layer: SafeMoEConfig(mlp_class_name="LLaMAMoE").mlp_class is SafeMoELayer
  </behavior>
  <action>
    Import safemoe.config.SafeMoEConfig (will raise ImportError — test file is RED).
    Also import safemoe.model.SafeMoELayer for the mlp_class test.
    Write four test functions with the exact assertions described in behavior.
    Do NOT create tests/safemoe/__init__.py — Phase 1 lesson: pytest namespace collision shadows source package. Leave absent.
    Each test function body uses the SMALL_CONFIG dims defined above where a model is needed.
    Run pytest tests/safemoe/test_config.py -x — expect collection errors (RED). This is correct.
  </action>
  <verify>
    <automated>pytest tests/safemoe/test_config.py --collect-only 2>&amp;1 | grep -E "(ERROR|ImportError|collected)" | head -20</automated>
  </verify>
  <done>File exists, pytest attempts collection, fails with ImportError on safemoe.config (RED state confirmed)</done>
</task>

<task type="tdd">
  <name>Task 2: Write RED test stubs for SafeMoELayer and experiment YAML (test_model.py + YAML)</name>
  <files>tests/safemoe/test_model.py, safemoe/configs/safemoe-tinystories.yaml</files>
  <behavior>
    test_model.py stubs:
    - test_safemoe_layer_structure: Build GPT model with SafeMoEConfig; assert isinstance(model.transformer.h[0].mlp, SafeMoELayer) for each block; assert each SafeMoELayer has n_expert=4 experts in self.experts
    - test_harmful_expert_init_random: SafeMoELayer built with harmful_expert_indices=[0,1] and init_strategy="random"; assert expert[0].fc_1.weight is NOT equal (allclose) to expert[2].fc_1.weight (random init differs from std experts)
    - test_harmful_expert_init_copy: SafeMoELayer built with init_strategy="copy"; assert expert[0].fc_1.weight allclose to the nearest std expert's fc_1.weight (copy produces identical weights)
    - test_activation_masking_flag_exists: SafeMoELayer instance has _activation_masking_enabled attribute, default False
  </behavior>
  <action>
    Create tests/safemoe/test_model.py importing SafeMoEConfig, SafeMoELayer from safemoe.model.
    Use litgpt.GPT to build a model: model = litgpt.GPT(config) where config is SafeMoEConfig(**SMALL_CONFIG).
    For harmful_expert_init tests, use SafeMoEConfig(**SMALL_CONFIG) directly constructing a SafeMoELayer.
    Create safemoe/configs/ directory if absent, then write safemoe/configs/safemoe-tinystories.yaml with the fields from RESEARCH.md YAML guidance:
      name: safemoe-tinystories
      block_size: 256
      padded_vocab_size: 32000
      n_layer: 4
      n_head: 4
      n_query_groups: 4
      n_embd: 128
      head_size: 32
      rotary_percentage: 1.0
      parallel_residual: false
      bias: false
      norm_class_name: RMSNorm
      mlp_class_name: LLaMAMoE
      moe_intermediate_size: 256
      n_expert: 8
      n_expert_per_token: 2
      harmful_expert_indices: [0, 1]
      num_harmful_experts: 2
      harmful_attn_heads: []
    Run pytest tests/safemoe/test_model.py --collect-only — expect ImportError (RED).
  </action>
  <verify>
    <automated>pytest tests/safemoe/test_model.py --collect-only 2>&amp;1 | grep -E "(ERROR|ImportError|collected)" | head -20</automated>
  </verify>
  <done>test_model.py exists and fails RED; safemoe-tinystories.yaml exists and is valid YAML with mlp_class_name: LLaMAMoE</done>
</task>

<task type="tdd">
  <name>Task 3: Write RED test stubs for HarmfulParamRegistry (test_registry.py)</name>
  <files>tests/safemoe/test_registry.py</files>
  <behavior>
    - test_registry_theta_harmful_contains_expert_params: Build model with SafeMoEConfig(harmful_expert_indices=[0,1]); registry = HarmfulParamRegistry(model, config); confirm all parameters whose name matches transformer.h.*.mlp.experts.0.* or transformer.h.*.mlp.experts.1.* are in parameters_by_type('theta_harmful')
    - test_registry_exhaustive_coverage: {id(p) for p in registry.parameters_by_type('theta_harmful')} | {id(p) for p in registry.parameters_by_type('theta_std')} == {id(p) for _, p in model.named_parameters()}
    - test_registry_non_overlapping: {id(p) for p in registry.parameters_by_type('theta_harmful')} & {id(p) for p in registry.parameters_by_type('theta_std')} == set()
    - test_registry_raises_on_invalid_config: Constructing HarmfulParamRegistry with a config that would produce theta_harmful ∩ theta_std != {} raises ValueError (simulate by monkey-patching; OR simply assert ValueError is NOT raised on a valid config — valid construction succeeds without error)
    - test_param_groups_disjoint: parameters_by_type('theta_harmful') and parameters_by_type('theta_std') are disjoint by parameter identity (id() comparison, same as test_registry_non_overlapping but named for MASK-03 traceability)
  </behavior>
  <action>
    Create tests/safemoe/test_registry.py importing HarmfulParamRegistry from safemoe.masking.
    Also import SafeMoEConfig from safemoe.config and litgpt.GPT.
    Use SMALL_CONFIG dims. Build model once in a pytest fixture or at test function level (no conftest.py needed for Phase 2 — keep it local).
    Use id(p) for all set comparisons per RESEARCH.md Pitfall 2 guidance.
    Run pytest tests/safemoe/test_registry.py --collect-only — expect ImportError (RED).
  </action>
  <verify>
    <automated>pytest tests/safemoe/test_registry.py --collect-only 2>&amp;1 | grep -E "(ERROR|ImportError|collected)" | head -20</automated>
  </verify>
  <done>test_registry.py exists and fails RED (ImportError on safemoe.masking)</done>
</task>

<task type="tdd">
  <name>Task 4: Write RED test stubs for GradientMasker and ActivationMasker (test_masking.py)</name>
  <files>tests/safemoe/test_masking.py</files>
  <behavior>
    - test_gradient_masker_zeroes_theta_std_grads: Build model, run forward+backward on random input; call gradient_masker.mask(); assert all(p.grad is None for p in registry.parameters_by_type('theta_std')); assert all(p.grad is not None for p in registry.parameters_by_type('theta_harmful'))
    - test_activation_masker_zeroes_harmful_expert_output: Build model; enable activation_masker; run forward on random input and extract per-expert contributions; assert harmful expert contribution is exactly zero; assert std expert contribution is nonzero
    - test_masking_invariants_combined: Run D_harmful step (backward + gradient_masker.mask()); run D_std step (activation_masker.enable(), forward, activation_masker.disable()); assert neither step corrupts the other's gradient state
    - test_set_to_none_adam_state_integrity: Construct dual AdamW (one for theta_harmful params, one for theta_std params) with zero_grad(set_to_none=True); run one D_harmful step with gradient masking; assert theta_std optimizer state remains zero/None (no momentum accumulated on masked params)
  </behavior>
  <action>
    Create tests/safemoe/test_masking.py importing HarmfulParamRegistry, GradientMasker, ActivationMasker from safemoe.masking.
    Also import SafeMoEConfig from safemoe.config and litgpt.GPT.
    For test_activation_masker_zeroes_harmful_expert_output: the test needs access to per-expert contributions. SafeMoELayer.forward() must expose a way to verify harmful expert outputs are zero. Write the test to check model output with masker enabled vs disabled — or mock intermediate state. Keep test simple: with masker enabled, model should still produce valid output (not crash), AND a direct check that SafeMoELayer._activation_masking_enabled is True when masker is enabled.
    For test_set_to_none_adam_state_integrity: construct two AdamW optimizers from registry.parameters_by_type(); run backward + gradient_masker.mask(); step only harmful_optimizer; assert theta_std optimizer has no non-None state for any param.
    Run pytest tests/safemoe/test_masking.py --collect-only — expect ImportError (RED).
  </action>
  <verify>
    <automated>pytest tests/safemoe/test_masking.py --collect-only 2>&amp;1 | grep -E "(ERROR|ImportError|collected)" | head -20</automated>
  </verify>
  <done>test_masking.py exists and fails RED (ImportError on safemoe.masking)</done>
</task>

</tasks>

<verification>
All four test files are written and fail RED (ImportError, not passing by accident).
YAML config is valid and loadable.

```bash
pytest tests/safemoe/ --collect-only 2>&1 | tail -5
# Expected: collection errors on all four files (RED state)
python -c "import yaml; yaml.safe_load(open('safemoe/configs/safemoe-tinystories.yaml'))"
# Expected: no error (valid YAML)
```
</verification>

<success_criteria>
- tests/safemoe/test_config.py, test_model.py, test_registry.py, test_masking.py all exist
- All four files fail RED (ImportError — safemoe.config, safemoe.model, safemoe.masking do not exist yet)
- safemoe/configs/safemoe-tinystories.yaml exists with mlp_class_name: LLaMAMoE, n_expert: 8, harmful_expert_indices: [0, 1]
- No tests/safemoe/__init__.py created (namespace collision prevention)
</success_criteria>

<output>
After completion, create `.planning/phases/02-model-architecture-masking/02-01-SUMMARY.md`
</output>
