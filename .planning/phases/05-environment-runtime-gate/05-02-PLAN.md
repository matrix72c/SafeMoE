---
phase: 05-environment-runtime-gate
plan: "02"
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - tests/safemoe/test_phase5_runtime_gate.py
  - safemoe/pretrain.py
  - .planning/phases/05-environment-runtime-gate/05-runtime-envelope.md
autonomous: false
requirements: [ENV-01, ENV-02]

must_haves:
  truths:
    - "The blessed `python -m safemoe pretrain` gate run emits machine-readable startup, first-step, throughput, and peak-memory values for the one-step BF16 baseline."
    - "Phase 5 ends with one committed markdown artifact in the phase directory recording the exact command, checkpoint path, topology, precision, seed, storage footprint, startup seconds, first-step seconds, tokens/sec, peak memory, and pass/fail status."
    - "The gate run stays on the real `Qwen3-30B-A3B-Base` pretrain path with `devices=4`, `num_nodes=1`, `bf16-true`, `train.max_seq_length=1024`, and `train.max_tokens=4096`."
  artifacts:
    - path: tests/safemoe/test_phase5_runtime_gate.py
      provides: "Coverage for runtime-gate metric output and one-step token budget assumptions"
      min_lines: 90
    - path: safemoe/pretrain.py
      provides: "Phase 5 runtime metric instrumentation and exact metric key printouts"
      contains: "PHASE5_GATE_STARTUP_SECONDS"
    - path: .planning/phases/05-environment-runtime-gate/05-runtime-envelope.md
      provides: "Canonical runtime envelope report"
      contains: "Status:"
  key_links:
    - from: "safemoe/pretrain.py"
      to: ".planning/phases/05-environment-runtime-gate/05-runtime-envelope.md"
      via: "stdout metric keys copied verbatim into the report"
      pattern: "PHASE5_GATE_"
    - from: "safemoe/configs/safemoe-qwen-phase5-gate.yaml"
      to: "safemoe/pretrain.py fit()"
      via: "one optimizer step encoded by max_tokens/world_size/max_seq_length"
      pattern: "max_tokens: 4096"
    - from: "python -m safemoe pretrain"
      to: "checkpoints/Qwen3-30B-A3B-Base"
      via: "--initial_checkpoint_dir and --tokenizer_dir both point at the pinned checkpoint"
      pattern: "Qwen3-30B-A3B-Base"
---

<objective>
Complete ENV-01 and ENV-02 by instrumenting the direct pretrain path with exact Phase 5 runtime metrics, then run the blessed BF16 one-step gate on the approved Qwen topology and commit the canonical runtime envelope report.

Purpose: Phase 6-10 should inherit a real storage, memory, and throughput budget instead of a guessed one.
Output: Runtime-gate metric coverage in tests, metric printouts in `safemoe/pretrain.py`, and `.planning/phases/05-environment-runtime-gate/05-runtime-envelope.md`.
</objective>

<execution_context>
@/home/jincheng1/.codex/get-shit-done/workflows/execute-plan.md
@/home/jincheng1/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-environment-runtime-gate/05-CONTEXT.md
@.planning/phases/05-environment-runtime-gate/05-RESEARCH.md
@.planning/phases/05-environment-runtime-gate/05-01-PLAN.md
@safemoe/pretrain.py
@safemoe/configs/safemoe-qwen-phase5-gate.yaml
@tests/safemoe/test_phase5_runtime_gate.py
@checkpoints/Qwen3-30B-A3B-Base/model_config.yaml

<interfaces>
Existing Phase 5 runtime contract from Plan 05-01:
```yaml
model_name: Qwen3-30B-A3B-Base
devices: 4
num_nodes: 1
seed: 42
train:
  micro_batch_size: 1
  global_batch_size: 4
  max_seq_length: 1024
  max_tokens: 4096
```

Current end-of-run metrics already in `safemoe/pretrain.py`:
```python
fabric.print(f"| - Training Time : {(time.perf_counter() - train_time):.2f} s")
fabric.print(f"| - Tok/sec       : {total_tokens / train_time:.2f} tok/s")
memory_used = torch.cuda.max_memory_allocated() / 1e9
```

Required new Phase 5 metric keys to print exactly once each:
```text
PHASE5_GATE_STARTUP_SECONDS=
PHASE5_GATE_FIRST_STEP_SECONDS=
PHASE5_GATE_FIRST_STEP_TOKENS_PER_SEC=
PHASE5_GATE_PEAK_MEMORY_GB=
```

Canonical report headings to create:
```markdown
# Phase 5 Runtime Envelope
Status:
Command:
Checkpoint:
Topology:
Precision:
Seed:
Storage footprint:
Timings:
Runtime:
Peak memory:
Warnings and notes:
Replay:
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add one-step runtime-gate metric instrumentation and coverage</name>
  <read_first>
tests/safemoe/test_phase5_runtime_gate.py
safemoe/pretrain.py
safemoe/configs/safemoe-qwen-phase5-gate.yaml
checkpoints/Qwen3-30B-A3B-Base/model_config.yaml
  </read_first>
  <files>tests/safemoe/test_phase5_runtime_gate.py, safemoe/pretrain.py</files>
  <behavior>
    - The one-step gate path prints `PHASE5_GATE_STARTUP_SECONDS=<float>` after checkpoint load and before the training loop begins.
    - The first completed optimizer step prints `PHASE5_GATE_FIRST_STEP_SECONDS=<float>` and `PHASE5_GATE_FIRST_STEP_TOKENS_PER_SEC=<float>`.
    - On CUDA runs, the gate also prints `PHASE5_GATE_PEAK_MEMORY_GB=<float>` using `torch.cuda.max_memory_allocated() / 1e9`; on non-CUDA runs it prints `PHASE5_GATE_PEAK_MEMORY_GB=NA`.
    - The instrumentation uses the actual one-step gate contract from the config: `devices=4`, `global_batch_size=4`, `micro_batch_size=1`, `max_seq_length=1024`, `max_tokens=4096`.
  </behavior>
  <action>
Extend `tests/safemoe/test_phase5_runtime_gate.py` and `safemoe/pretrain.py` so the Phase 5 runtime gate emits deterministic metric keys that the report task can scrape from stdout.

Implementation target in `safemoe/pretrain.py`:

1. Add module-level metric key constants:
```python
PHASE5_GATE_STARTUP_KEY = "PHASE5_GATE_STARTUP_SECONDS"
PHASE5_GATE_FIRST_STEP_KEY = "PHASE5_GATE_FIRST_STEP_SECONDS"
PHASE5_GATE_TOKENS_PER_SEC_KEY = "PHASE5_GATE_FIRST_STEP_TOKENS_PER_SEC"
PHASE5_GATE_PEAK_MEMORY_KEY = "PHASE5_GATE_PEAK_MEMORY_GB"
```

2. In `main(...)`, measure startup time from immediately before `with fabric.init_module(empty_init=True):` until immediately after `fabric.load_raw(initial_checkpoint_dir / "lit_model.pth", model)` returns. Print:
```python
fabric.print(f"{PHASE5_GATE_STARTUP_KEY}={startup_seconds:.4f}")
```

3. In `fit(...)`, measure the first optimizer step only:
- start the timer immediately before the outer `while state["iter_num"] < max_iters:` loop enters its first iteration
- stop the timer immediately after the first `optimizer.step()` completes
- compute first-step tokens/sec as:
```python
first_step_tokens = train.global_batch_size * model.max_seq_length
first_step_tokens_per_sec = first_step_tokens / first_step_seconds
```
- print exactly:
```python
fabric.print(f"{PHASE5_GATE_FIRST_STEP_KEY}={first_step_seconds:.4f}")
fabric.print(f"{PHASE5_GATE_TOKENS_PER_SEC_KEY}={first_step_tokens_per_sec:.2f}")
```

4. For peak memory:
- if `fabric.device.type == "cuda"`, call `torch.cuda.reset_peak_memory_stats()` just before the first-step timer starts and `torch.cuda.synchronize()` before reading the value
- after the first optimizer step, print either
```python
fabric.print(f"{PHASE5_GATE_PEAK_MEMORY_KEY}={torch.cuda.max_memory_allocated() / 1e9:.2f}")
```
or
```python
fabric.print(f"{PHASE5_GATE_PEAK_MEMORY_KEY}=NA")
```
when CUDA is unavailable

5. Add tests that assert the exact metric key strings exist in stdout or captured prints. Keep them CPU-safe by allowing `PHASE5_GATE_PEAK_MEMORY_GB=NA` on non-CUDA hosts.

Do not broaden this into a generic profiler. The only new outputs are the four `PHASE5_GATE_*` lines above.
  </action>
  <acceptance_criteria>
- `rg -n "PHASE5_GATE_STARTUP_KEY|PHASE5_GATE_FIRST_STEP_KEY|PHASE5_GATE_TOKENS_PER_SEC_KEY|PHASE5_GATE_PEAK_MEMORY_KEY" safemoe/pretrain.py` returns all four constants.
- `rg -n "PHASE5_GATE_STARTUP_SECONDS=|PHASE5_GATE_FIRST_STEP_SECONDS=|PHASE5_GATE_FIRST_STEP_TOKENS_PER_SEC=|PHASE5_GATE_PEAK_MEMORY_GB=" safemoe/pretrain.py` returns all four exact print formats.
- `rg -n "PHASE5_GATE_STARTUP_SECONDS|PHASE5_GATE_FIRST_STEP_SECONDS|PHASE5_GATE_FIRST_STEP_TOKENS_PER_SEC|PHASE5_GATE_PEAK_MEMORY_GB" tests/safemoe/test_phase5_runtime_gate.py` returns assertions for all four metrics.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/safemoe/test_phase5_runtime_gate.py tests/safemoe/test_pretrain.py -x</automated>
  </verify>
  <done>
    The direct pretrain path emits the exact four Phase 5 metric keys needed for the envelope artifact, and the added tests prove the output contract without requiring a GPU.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create the canonical runtime-envelope report template in the phase directory</name>
  <read_first>
.planning/phases/05-environment-runtime-gate/05-RESEARCH.md
.planning/phases/05-environment-runtime-gate/05-CONTEXT.md
safemoe/configs/safemoe-qwen-phase5-gate.yaml
  </read_first>
  <files>.planning/phases/05-environment-runtime-gate/05-runtime-envelope.md</files>
  <action>
Create `.planning/phases/05-environment-runtime-gate/05-runtime-envelope.md` with the exact skeleton below. Leave metric values blank except where the command/topology is already known.

```markdown
# Phase 5 Runtime Envelope

Status: PENDING
Date:
Command: `python -m safemoe pretrain --config safemoe/configs/safemoe-qwen-phase5-gate.yaml --precision bf16-true --initial_checkpoint_dir checkpoints/Qwen3-30B-A3B-Base --tokenizer_dir checkpoints/Qwen3-30B-A3B-Base --out_dir out/phase5-runtime-gate Qwen3-30B-A3B-Base`
Checkpoint: `checkpoints/Qwen3-30B-A3B-Base`
Topology: `4 GPU x 1 node`, `FSDPStrategy(auto_wrap_policy={Block}, state_dict_type="full", sharding_strategy="HYBRID_SHARD")`
Precision: `bf16-true`
Seed: `42`

## Storage footprint
- `lit_model.pth`:

## Timings
- `PHASE5_GATE_STARTUP_SECONDS`:
- `PHASE5_GATE_FIRST_STEP_SECONDS`:

## Runtime
- `PHASE5_GATE_FIRST_STEP_TOKENS_PER_SEC`:

## Peak memory
- `PHASE5_GATE_PEAK_MEMORY_GB`:

## Warnings and notes
- CUDA host:
- Data cache root: `data/.cache/Qwen3-30B-A3B-Base/0-25`
- Observed warnings:

## Replay
1. Ensure `data/.cache/Qwen3-30B-A3B-Base/0-25` contains `D_std/train`, `D_harmful/train`, `D_unlabeled/train`, `D_std/val`, and `D_harmful/val`.
2. Run the command above on the blessed 4-GPU BF16 host.
3. Copy the four `PHASE5_GATE_*` lines and `du -sh checkpoints/Qwen3-30B-A3B-Base/lit_model.pth` into this file.
4. Change `Status:` to `PASS` or `FAIL` and summarize any warnings.
```

Do not add alternative commands, fallback precisions, or extra benchmark matrix sections.
  </action>
  <acceptance_criteria>
- `rg -n "^Status: PENDING$|^Command: `python -m safemoe pretrain --config safemoe/configs/safemoe-qwen-phase5-gate.yaml --precision bf16-true --initial_checkpoint_dir checkpoints/Qwen3-30B-A3B-Base --tokenizer_dir checkpoints/Qwen3-30B-A3B-Base --out_dir out/phase5-runtime-gate Qwen3-30B-A3B-Base`$|^Topology: `4 GPU x 1 node`, `FSDPStrategy\\(auto_wrap_policy=\\{Block\\}, state_dict_type=\"full\", sharding_strategy=\"HYBRID_SHARD\"\\)`$|PHASE5_GATE_STARTUP_SECONDS|PHASE5_GATE_FIRST_STEP_SECONDS|PHASE5_GATE_FIRST_STEP_TOKENS_PER_SEC|PHASE5_GATE_PEAK_MEMORY_GB" .planning/phases/05-environment-runtime-gate/05-runtime-envelope.md` returns the exact contract."
  </acceptance_criteria>
  <verify>
    <automated>rg -n "^Status: PENDING$|PHASE5_GATE_STARTUP_SECONDS|PHASE5_GATE_FIRST_STEP_SECONDS|PHASE5_GATE_FIRST_STEP_TOKENS_PER_SEC|PHASE5_GATE_PEAK_MEMORY_GB" .planning/phases/05-environment-runtime-gate/05-runtime-envelope.md</automated>
  </verify>
  <done>
    The canonical Phase 5 artifact file exists with the exact blessed command and blank metric slots ready to be filled from the real gate run.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Run the blessed BF16 gate on the GPU host and finalize the envelope report</name>
  <read_first>
.planning/phases/05-environment-runtime-gate/05-runtime-envelope.md
safemoe/configs/safemoe-qwen-phase5-gate.yaml
safemoe/pretrain.py
  </read_first>
  <files>.planning/phases/05-environment-runtime-gate/05-runtime-envelope.md</files>
  <action>
Execute the exact blessed Phase 5 BF16 command on the GPU host that will run milestone experiments:

`python -m safemoe pretrain --config safemoe/configs/safemoe-qwen-phase5-gate.yaml --precision bf16-true --initial_checkpoint_dir checkpoints/Qwen3-30B-A3B-Base --tokenizer_dir checkpoints/Qwen3-30B-A3B-Base --out_dir out/phase5-runtime-gate Qwen3-30B-A3B-Base`

Before running it:
- confirm the prepared cache root is `data/.cache/Qwen3-30B-A3B-Base/0-25`
- confirm that cache root contains `D_std/train`, `D_harmful/train`, `D_unlabeled/train`, `D_std/val`, and `D_harmful/val`
- record `du -sh checkpoints/Qwen3-30B-A3B-Base/lit_model.pth`

After the command completes or fails:
- treat this run as the requirement-closing proof for both `ENV-01` and `ENV-02`, because it loads the existing `checkpoints/Qwen3-30B-A3B-Base` checkpoint through the real direct-Qwen pretrain path and then attempts the first optimizer step
- copy the four stdout metric lines beginning with `PHASE5_GATE_` into `.planning/phases/05-environment-runtime-gate/05-runtime-envelope.md`
- set `Status: PASS` if one optimizer step completed
- set `Status: FAIL` if the run stopped on missing files, config/tokenizer mismatch, BF16 failure, startup failure, or first-step failure
- fill the `CUDA host`, `Storage footprint`, and `Observed warnings` fields with the concrete values from the run
  </action>
  <what-built>Phase 5 now has an exact BF16 gate config, direct-path preflight checks, and four machine-readable runtime metrics printed by `python -m safemoe pretrain`.</what-built>
  <how-to-verify>
1. Use the GPU host that will run milestone experiments. This plan assumes the blessed topology is 4 GPUs on 1 node with BF16 support.
2. Confirm the TinyStories cache exists at `data/.cache/Qwen3-30B-A3B-Base/0-25` and contains `D_std/train`, `D_harmful/train`, `D_unlabeled/train`, `D_std/val`, and `D_harmful/val`.
3. Run `du -sh checkpoints/Qwen3-30B-A3B-Base/lit_model.pth`.
4. Run:
   `python -m safemoe pretrain --config safemoe/configs/safemoe-qwen-phase5-gate.yaml --precision bf16-true --initial_checkpoint_dir checkpoints/Qwen3-30B-A3B-Base --tokenizer_dir checkpoints/Qwen3-30B-A3B-Base --out_dir out/phase5-runtime-gate Qwen3-30B-A3B-Base`
5. Wait for startup plus the first completed optimizer step. Capture the four stdout lines:
   `PHASE5_GATE_STARTUP_SECONDS=...`
   `PHASE5_GATE_FIRST_STEP_SECONDS=...`
   `PHASE5_GATE_FIRST_STEP_TOKENS_PER_SEC=...`
   `PHASE5_GATE_PEAK_MEMORY_GB=...`
6. Update `.planning/phases/05-environment-runtime-gate/05-runtime-envelope.md`:
   - set `Status: PASS` if the command completed one optimizer step
   - set `Status: FAIL` if it stopped on checkpoint/config/tokenizer/BF16/startup/optimizer-step failure
   - fill storage footprint, timings, runtime, peak memory, CUDA host, and observed warnings
7. Save the edited markdown in the phase directory as the canonical artifact.
  </how-to-verify>
  <acceptance_criteria>
- `rg -n "^Status: PASS$|^Status: FAIL$" .planning/phases/05-environment-runtime-gate/05-runtime-envelope.md` returns exactly one final status.
- `rg -n "PHASE5_GATE_STARTUP_SECONDS|PHASE5_GATE_FIRST_STEP_SECONDS|PHASE5_GATE_FIRST_STEP_TOKENS_PER_SEC|PHASE5_GATE_PEAK_MEMORY_GB" .planning/phases/05-environment-runtime-gate/05-runtime-envelope.md` returns all four populated metric lines.
- `rg -n "^Topology: `4 GPU x 1 node`|^Precision: `bf16-true`$|^Checkpoint: `checkpoints/Qwen3-30B-A3B-Base`$" .planning/phases/05-environment-runtime-gate/05-runtime-envelope.md` preserves the blessed gate contract.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "^Status: PASS$|^Status: FAIL$|PHASE5_GATE_STARTUP_SECONDS|PHASE5_GATE_FIRST_STEP_SECONDS|PHASE5_GATE_FIRST_STEP_TOKENS_PER_SEC|PHASE5_GATE_PEAK_MEMORY_GB|^Topology: `4 GPU x 1 node`|^Precision: `bf16-true`$|^Checkpoint: `checkpoints/Qwen3-30B-A3B-Base`$" .planning/phases/05-environment-runtime-gate/05-runtime-envelope.md</automated>
  </verify>
  <done>
    The Phase 5 runtime-envelope artifact is filled with real BF16 measurements from the blessed topology and clearly records whether the existing checkpoint both loaded successfully and completed the one-step gate.
  </done>
  <resume-signal>Type "approved" after the report is filled, or paste the failure details that should be captured as Phase 5 blockers.</resume-signal>
</task>

</tasks>

<verification>
Before pausing for the GPU checkpoint, run `pytest tests/safemoe/test_phase5_runtime_gate.py tests/safemoe/test_pretrain.py -x` and verify `.planning/phases/05-environment-runtime-gate/05-runtime-envelope.md` contains the exact command and metric keys. The blocking gate is the real BF16 pretrain command from Task 3.
</verification>

<success_criteria>
- `ENV-02` has exact runtime-metric printouts and a committed markdown artifact path.
- The Phase 5 report records command, topology, storage, startup time, first-step time, tokens/sec, and peak memory for the blessed run.
- The plan stops at a single blocking GPU-host checkpoint instead of inventing CPU or mixed-precision substitutes.
</success_criteria>

<output>
After completion, create `.planning/phases/05-environment-runtime-gate/05-02-SUMMARY.md`
</output>
