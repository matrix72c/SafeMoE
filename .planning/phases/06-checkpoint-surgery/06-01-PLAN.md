---
phase: 06-checkpoint-surgery
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/safemoe/test_checkpoint_surgery.py
  - safemoe/interventions/__init__.py
  - safemoe/interventions/manifest.py
  - safemoe/interventions/planner.py
autonomous: true
requirements:
  - INIT-01
must_haves:
  truths:
    - "Researcher can build a manifest from explicit source experts, target harmful experts, source heads, target harmful heads, seed, and nonzero noise scale."
    - "The manifest can be serialized to JSON and reloaded without changing source bundle, target layout, seed, or noise scale."
    - "The run metadata exposes inspectable derived router-column source/target pairs computed from the expert mapping without requiring redundant user-specified router inputs."
    - "One manifest represents exactly one coherent source bundle, so expert and head selections cannot be assembled from different source-bundle identifiers."
  artifacts:
    - path: "tests/safemoe/test_checkpoint_surgery.py"
      provides: "Unit coverage for manifest validation, round-trip serialization, and deterministic planning."
      min_lines: 80
    - path: "safemoe/interventions/manifest.py"
      provides: "Manifest dataclasses, validation, JSON persistence, and stable manifest_id generation."
      exports: ["SourceBundle", "TargetLayout", "InterventionManifest", "save_manifest", "load_manifest"]
    - path: "safemoe/interventions/planner.py"
      provides: "Pure deterministic planner that converts explicit source/target selections into a complete intervention manifest without redundant router mappings."
      exports: ["plan_intervention_manifest"]
    - path: "safemoe/interventions/manifest.py"
      provides: "Helper for deriving inspectable router-column source/target pairs from expert mappings for provenance and verification artifacts."
      exports: ["derived_router_column_pairs"]
  key_links:
    - from: "safemoe/interventions/planner.py"
      to: "safemoe/interventions/manifest.py"
      via: "plan_intervention_manifest returns InterventionManifest"
      pattern: "InterventionManifest\\("
    - from: "safemoe/interventions/manifest.py"
      to: "safemoe/interventions/planner.py"
      via: "derived router-column pairs are computed from planner expert mappings for inspectable provenance"
      pattern: "derived_router_column_pairs|source_expert_indices"
---

<objective>
Define the manifest contract and deterministic intervention planner that Phase 6 will treat as the source of truth for checkpoint surgery.

Purpose: Lock the exact provenance record before any tensor mutation code exists so INIT-01 is satisfied independently and downstream surgery logic has a stable contract.
Output: A tested `InterventionManifest` schema, JSON round-trip helpers, and a deterministic planner under `safemoe/interventions/`.
</objective>

<execution_context>
@/home/jincheng1/.codex/get-shit-done/workflows/execute-plan.md
@/home/jincheng1/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-checkpoint-surgery/06-CONTEXT.md
@.planning/phases/06-checkpoint-surgery/06-RESEARCH.md
@.planning/phases/06-checkpoint-surgery/06-VALIDATION.md
@safemoe/config.py
@safemoe/pretrain.py
@checkpoints/Qwen3-30B-A3B-Base/model_config.yaml

<interfaces>
From `safemoe/config.py`:
```python
@dataclass
class SafeMoEConfig(BaseConfig):
    harmful_expert_indices: list = field(default_factory=list)
    harmful_attn_heads: list = field(default_factory=list)
    num_harmful_experts: int = 0
```

From `safemoe/pretrain.py`:
```python
def ensure_safemoe_config(config: Union[Config, SafeMoEConfig]) -> SafeMoEConfig: ...
def save_checkpoint(fabric, state, tokenizer_dir, checkpoint_file, include_optimizer: bool = True): ...
```

Pinned Qwen dimensions from `checkpoints/Qwen3-30B-A3B-Base/model_config.yaml`:
```yaml
name: Qwen3-30B-A3B-Base
n_layer: 48
n_expert: 128
n_head: 32
n_query_groups: 4
head_size: 128
mlp_class_name: LLaMAMoE
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the Phase 6 manifest test contract and schema module</name>
  <files>tests/safemoe/test_checkpoint_surgery.py, safemoe/interventions/__init__.py, safemoe/interventions/manifest.py</files>
  <read_first>.planning/phases/06-checkpoint-surgery/06-CONTEXT.md, .planning/phases/06-checkpoint-surgery/06-RESEARCH.md, .planning/phases/06-checkpoint-surgery/06-VALIDATION.md, safemoe/config.py, safemoe/ablate.py, tests/safemoe/test_ablate.py</read_first>
  <behavior>
    - Test 1: Constructing a manifest with `noise_scale=0` raises `ValueError` containing `noise_scale must be > 0`.
    - Test 2: Saving then loading a manifest preserves `source_bundle_id`, `source_expert_indices`, `target_harmful_expert_indices`, `source_attn_head_indices`, `target_harmful_attn_heads`, `seed`, and `noise_scale`.
    - Test 3: The manifest id and output directory stem are stable for the same payload and contain `Qwen3-30B-A3B-Base-surgery-`.
    - Test 4: `derived_router_column_pairs()` returns inspectable `(source_expert, target_expert)` pairs in manifest order without persisting redundant router input fields.
    - Test 5: Planner or manifest validation rejects mixed source layouts by raising `ValueError` when expert and head selections are provided under different source-bundle identifiers.
  </behavior>
  <action>Create `tests/safemoe/test_checkpoint_surgery.py` with concrete manifest-focused tests using a tiny synthetic `SafeMoEConfig`. Implement `safemoe/interventions/manifest.py` with dataclasses `SourceBundle`, `TargetLayout`, and `InterventionManifest`; require exact JSON fields `manifest_version`, `manifest_id`, `base_checkpoint_dir`, `base_checkpoint_name`, `source_bundle_id`, `seed`, `noise_scale`, `source_expert_indices`, `target_harmful_expert_indices`, `source_attn_head_indices`, `target_harmful_attn_heads`, and `output_checkpoint_dirname`. Define “one coherent source bundle” concretely as: one manifest has exactly one `source_bundle_id`, and both expert selections and head selections must belong to that same identifier; the planner API must reject inputs that attempt to provide separate expert/head bundle identifiers with different values. Do not persist `router_source_expert_indices` or `router_target_expert_indices` as user-specified manifest inputs; instead add `derived_router_column_pairs(manifest)` that returns ordered `(source_expert, target_expert)` pairs computed from `source_expert_indices` and `target_harmful_expert_indices` for inspectable provenance and later verification/report artifacts. Validate that source and target expert counts match, source and target head counts match, all target indices are unique, and `noise_scale > 0.0`. Implement `save_manifest(path, manifest)` and `load_manifest(path)` using JSON with `indent=2`. Export these symbols from `safemoe/interventions/__init__.py`.</action>
  <verify>
    <automated>pytest tests/safemoe/test_checkpoint_surgery.py -k "manifest or serialization" -x</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "noise_scale must be > 0" tests/safemoe/test_checkpoint_surgery.py safemoe/interventions/manifest.py` finds the exact validation message in both test expectation and implementation.
    - `rg -n "source_bundle_id|coherent source bundle|mixed source" tests/safemoe/test_checkpoint_surgery.py safemoe/interventions/manifest.py safemoe/interventions/planner.py` finds the shared-bundle contract and rejection test.
    - `rg -n "output_checkpoint_dirname" safemoe/interventions/manifest.py tests/safemoe/test_checkpoint_surgery.py` finds the persisted field name.
    - `rg -n "derived_router_column_pairs" safemoe/interventions/manifest.py tests/safemoe/test_checkpoint_surgery.py` finds the inspectable derived router-pair helper and test coverage.
    - `rg -n "router_source_expert_indices|router_target_expert_indices" safemoe/interventions/manifest.py safemoe/interventions/planner.py tests/safemoe/test_checkpoint_surgery.py` returns no matches.
    - `pytest tests/safemoe/test_checkpoint_surgery.py -k "manifest or serialization" -x` exits 0.
  </acceptance_criteria>
  <done>The repo contains a concrete manifest schema with JSON persistence and passing unit coverage for validation and round-trip behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the deterministic intervention planner</name>
  <files>tests/safemoe/test_checkpoint_surgery.py, safemoe/interventions/planner.py, safemoe/interventions/__init__.py</files>
  <read_first>tests/safemoe/test_checkpoint_surgery.py, safemoe/interventions/manifest.py, safemoe/config.py, checkpoints/Qwen3-30B-A3B-Base/model_config.yaml</read_first>
  <behavior>
    - Test 1: Calling the planner twice with the same `SafeMoEConfig`, explicit source/target lists, `seed=1234`, and `noise_scale=0.001` returns the same `manifest_id`.
    - Test 2: The planner writes zipped `expert_pairs` and `head_pairs` in source-order, preserving the exact target layout from the call site.
    - Test 3: The planner output contains no explicit router mapping fields because router inheritance is implied by expert cloning and must be derived later during surgery and verification.
  </behavior>
  <action>Implement `plan_intervention_manifest()` in `safemoe/interventions/planner.py` as a pure function taking `config: SafeMoEConfig`, `base_checkpoint_dir: Path`, `source_bundle_id: str`, `source_expert_indices: list[int]`, `target_harmful_expert_indices: list[int]`, `source_attn_head_indices: list[int]`, `target_harmful_attn_heads: list[int]`, `seed: int`, and `noise_scale: float`. If the planner exposes lower-level expert/head bundle labels for validation, require them to both equal `source_bundle_id`; otherwise reject the call with `ValueError` when expert and head selections would come from different bundle identifiers. Build `expert_pairs` and `head_pairs` by zipping source and target lists in order. Do not add router-specific fields to the manifest payload; later code must derive router source/target columns directly from the expert source/target lists. Derive `base_checkpoint_name` from `config.name`, set `manifest_version` to `1`, and generate `manifest_id` from a stable SHA-256 hash of the JSON payload sorted by key, truncated to 12 lowercase hex chars. Set `output_checkpoint_dirname` exactly to `"{config.name}-surgery-{manifest_id}"`. Export `plan_intervention_manifest` from `safemoe/interventions/__init__.py`.</action>
  <verify>
    <automated>pytest tests/safemoe/test_checkpoint_surgery.py::test_manifest_planner_is_deterministic -x</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "sha256|manifest_id" safemoe/interventions/planner.py safemoe/interventions/manifest.py` shows stable hash-based manifest id generation.
    - `rg -n "output_checkpoint_dirname = f\"\\{config.name\\}-surgery-\\{manifest_id\\}\"|output_checkpoint_dirname.*surgery" safemoe/interventions/planner.py` finds the exact output naming contract.
    - `pytest tests/safemoe/test_checkpoint_surgery.py::test_manifest_planner_is_deterministic -x` exits 0.
  </acceptance_criteria>
  <done>Phase 6 has a deterministic manifest planner that records the explicit harmful layout and provenance needed by later surgery and verification steps.</done>
</task>

</tasks>

<verification>
Run `pytest tests/safemoe/test_checkpoint_surgery.py -x` after the plan completes. Confirm the manifest JSON contains explicit source/target expert lists, explicit source/target head lists, nonzero `noise_scale`, `seed`, and a stable `output_checkpoint_dirname`, does not contain persisted router mapping fields, and that `derived_router_column_pairs()` exposes inspectable router source/target pairs for provenance.
</verification>

<success_criteria>
`INIT-01` is satisfied: a researcher can generate and persist an intervention manifest with explicit expert/head selections, seed, and nonzero noise scale, while router inheritance remains implied by the expert mapping and is exposed through an inspectable derived router-pair record rather than redundant user-specified inputs.
</success_criteria>

<output>
After completion, create `.planning/phases/06-checkpoint-surgery/06-01-SUMMARY.md`
</output>
