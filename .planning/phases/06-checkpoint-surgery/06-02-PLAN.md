---
phase: 06-checkpoint-surgery
plan: "02"
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - tests/safemoe/test_checkpoint_surgery.py
  - safemoe/interventions/__init__.py
  - safemoe/interventions/surgery.py
  - safemoe/interventions/verify.py
  - safemoe/surgery.py
  - safemoe/__main__.py
autonomous: true
requirements:
  - INIT-02
  - INIT-03
must_haves:
  truths:
    - "Researcher can run one command that writes a post-surgery checkpoint directory under `checkpoints/` with manifest and verification artifacts."
    - "The saved checkpoint reloads with `GPT(SafeMoEConfig)` and exposes the harmful expert indices and harmful attention heads recorded in the manifest."
    - "Any manifest-mapping, reload, or shape-semantics mismatch fails the run before a blessed output directory is finalized."
  artifacts:
    - path: "tests/safemoe/test_checkpoint_surgery.py"
      provides: "Integration coverage for checkpoint surgery output, reload success, and verifier hard-failure behavior."
      min_lines: 140
    - path: "safemoe/interventions/surgery.py"
      provides: "State-dict mutation helpers for expert tensors, router columns, packed QKV head slices, and atomic checkpoint output finalization."
      exports: ["apply_intervention_manifest", "run_checkpoint_surgery"]
    - path: "safemoe/interventions/verify.py"
      provides: "Reload-based deterministic parity verification and report writers."
      exports: ["verify_intervention_output"]
    - path: "safemoe/surgery.py"
      provides: "Researcher-facing CLI entrypoint for manifest-backed checkpoint surgery."
      exports: ["setup"]
  key_links:
    - from: "safemoe/surgery.py"
      to: "safemoe/interventions/planner.py"
      via: "CLI constructs the InterventionManifest through plan_intervention_manifest before any mutation"
      pattern: "plan_intervention_manifest"
    - from: "safemoe/surgery.py"
      to: "safemoe/interventions/manifest.py"
      via: "CLI persists the exact manifest with save_manifest before verification/finalize"
      pattern: "save_manifest"
    - from: "safemoe/surgery.py"
      to: "safemoe/interventions/surgery.py"
      via: "CLI setup delegates to run_checkpoint_surgery"
      pattern: "run_checkpoint_surgery"
    - from: "safemoe/interventions/surgery.py"
      to: "safemoe/interventions/verify.py"
      via: "run_checkpoint_surgery verifies staging output before final rename"
      pattern: "verify_intervention_output"
    - from: "safemoe/interventions/verify.py"
      to: "safemoe/interventions/manifest.py"
      via: "verification reports inspectable derived router-column pairs from the shared manifest helper"
      pattern: "derived_router_column_pairs"
    - from: "safemoe/interventions/surgery.py"
      to: "checkpoints/"
      via: "atomic finalize from staging dir to output_checkpoint_dirname"
      pattern: "replace\\(|rename\\("
---

<objective>
Implement the actual Qwen checkpoint surgery path, verification pass, and researcher CLI that produces blessed post-surgery checkpoints for downstream warmup and evaluation.

Purpose: Close INIT-02 and INIT-03 by mutating the LitGPT checkpoint deterministically, preserving SafeMoE config semantics, and failing closed on any mismatch before a downstream artifact exists.
Output: A `python -m safemoe surgery ...` entrypoint that writes a loadable checkpoint directory plus `intervention_manifest.json`, `verification_report.json`, and `verification_summary.md`.
</objective>

<execution_context>
@/home/jincheng1/.codex/get-shit-done/workflows/execute-plan.md
@/home/jincheng1/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-checkpoint-surgery/06-CONTEXT.md
@.planning/phases/06-checkpoint-surgery/06-RESEARCH.md
@.planning/phases/06-checkpoint-surgery/06-VALIDATION.md
@.planning/phases/06-checkpoint-surgery/06-01-PLAN.md
@safemoe/pretrain.py
@safemoe/config.py
@safemoe/masking.py
@safemoe/ablate.py
@safemoe/__main__.py
@checkpoints/Qwen3-30B-A3B-Base/model_config.yaml

<interfaces>
From `safemoe/interventions/manifest.py` and `safemoe/interventions/planner.py` (created by 06-01):
```python
class InterventionManifest:
    manifest_version: int
    manifest_id: str
    base_checkpoint_dir: str
    base_checkpoint_name: str
    source_bundle_id: str
    seed: int
    noise_scale: float
    source_expert_indices: list[int]
    target_harmful_expert_indices: list[int]
    source_attn_head_indices: list[int]
    target_harmful_attn_heads: list[int]
    output_checkpoint_dirname: str

def plan_intervention_manifest(...) -> InterventionManifest: ...
```

From `safemoe/pretrain.py`:
```python
def ensure_safemoe_config(config: Union[Config, SafeMoEConfig]) -> SafeMoEConfig: ...
def save_checkpoint(fabric, state, tokenizer_dir, checkpoint_file, include_optimizer: bool = True): ...
```

From `safemoe/masking.py`:
```python
class HarmfulParamRegistry:
    def __init__(self, model: nn.Module, config: SafeMoEConfig) -> None: ...
    def parameters_by_type(self, split: str) -> list[nn.Parameter]: ...
```

Pinned tensor surfaces from Phase 6 context:
```text
transformer.h.{layer}.mlp.experts.{idx}.fc_1.weight
transformer.h.{layer}.mlp.experts.{idx}.fc_2.weight
transformer.h.{layer}.mlp.experts.{idx}.proj.weight
transformer.h.{layer}.mlp.gate.weight
transformer.h.{layer}.attn.qkv.weight
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement manifest-backed checkpoint mutation and blessed output writing</name>
  <files>tests/safemoe/test_checkpoint_surgery.py, safemoe/interventions/surgery.py, safemoe/surgery.py, safemoe/__main__.py, safemoe/interventions/__init__.py</files>
  <read_first>.planning/phases/06-checkpoint-surgery/06-01-PLAN.md, tests/safemoe/test_checkpoint_surgery.py, safemoe/interventions/manifest.py, safemoe/interventions/planner.py, safemoe/pretrain.py, safemoe/ablate.py, safemoe/__main__.py, checkpoints/Qwen3-30B-A3B-Base/model_config.yaml</read_first>
  <behavior>
    - Test 1: A synthetic checkpoint surgery run writes `lit_model.pth`, `model_config.yaml`, `intervention_manifest.json`, and copied tokenizer/config sidecars into `checkpoints/<base>-surgery-<manifest_id>/`.
    - Test 2: The saved `model_config.yaml` contains `harmful_expert_indices` equal to the manifest target experts, `harmful_attn_heads` equal to the manifest target heads, and `num_harmful_experts` equal to the length of `harmful_expert_indices`.
    - Test 3: Reloading the saved `lit_model.pth` into `GPT(SafeMoEConfig)` succeeds and the verification report confirms the target expert tensors, derived router columns, and target QKV slices match the manifest-declared source-to-target mapping semantics and shapes, using deterministic recomputation as the implementation method.
  </behavior>
  <action>Extend `tests/safemoe/test_checkpoint_surgery.py` with `test_surgery_writes_loadable_checkpoint_directory`. Implement `safemoe/interventions/surgery.py` with `apply_intervention_manifest(state_dict, config, manifest)` and `run_checkpoint_surgery(...)`. In `safemoe/surgery.py`, make the researcher-facing CLI parse `base_checkpoint_dir`, `source_bundle_id`, `source_expert_indices`, `target_harmful_expert_indices`, `source_attn_head_indices`, `target_harmful_attn_heads`, `seed`, `noise_scale`, and optional `output_root=Path("checkpoints")`, then construct the sole mutation contract by calling `plan_intervention_manifest(...)` from `safemoe/interventions/planner.py`. Persist that exact `InterventionManifest` with `save_manifest()` from `safemoe/interventions/manifest.py` as `intervention_manifest.json` in the staging output before verification/finalize, and pass the same manifest object into `run_checkpoint_surgery(...)`; do not duplicate manifest assembly inside surgery helpers. Load the base checkpoint from `base_checkpoint_dir / "lit_model.pth"` using `torch.load(..., map_location="cpu", weights_only=False)`. For every layer in `range(config.n_layer)`, clone expert tensors `fc_1.weight`, `fc_2.weight`, and `proj.weight` from each `source_expert_indices[i]` into `target_harmful_expert_indices[i]`. Derive router source/target columns directly from those same expert source/target lists and copy `state[gate_name][:, target_expert] = source_column + noise`; do not read or persist router-specific manifest fields. Clone fused attention rows inside `transformer.h.{layer}.attn.qkv.weight` using exact row slices `q = head_idx * head_size`, `k = n_head * head_size + (head_idx % n_query_groups) * head_size`, and `v = (n_head + n_query_groups) * head_size + (head_idx % n_query_groups) * head_size`. Generate all noise from `torch.Generator(device="cpu").manual_seed(manifest.seed)` and apply the same `noise_scale` to experts, derived router columns, and QKV slices in one deterministic order: iterate layers ascending, then expert pairs in manifest order for `fc_1.weight`, `fc_2.weight`, `proj.weight`, then router columns, then head pairs in manifest order for Q, K, and V slices. Update the saved config to `SafeMoEConfig` with `harmful_expert_indices = manifest.target_harmful_expert_indices`, `harmful_attn_heads = manifest.target_harmful_attn_heads`, and `num_harmful_experts = len(manifest.target_harmful_expert_indices)`. Write outputs to a staging directory named `<output_root>/<output_checkpoint_dirname>.tmp`, copy sidecars `tokenizer.json`, `tokenizer_config.json`, `config.json`, and `generation_config.json`, then wire `"surgery": surgery_fn` into `safemoe/__main__.py` and export the public functions from `safemoe/interventions/__init__.py`.</action>
  <verify>
    <automated>pytest tests/safemoe/test_checkpoint_surgery.py::test_surgery_writes_loadable_checkpoint_directory -x</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "\"surgery\": surgery_fn|surgery_fn" safemoe/__main__.py` finds the new CLI command wiring.
    - `rg -n "plan_intervention_manifest|save_manifest" safemoe/surgery.py safemoe/interventions/surgery.py tests/safemoe/test_checkpoint_surgery.py` finds the explicit manifest-first wiring.
    - `rg -n "intervention_manifest.json|generation_config.json|tokenizer_config.json" safemoe/interventions/surgery.py tests/safemoe/test_checkpoint_surgery.py` finds exact artifact names and sidecar copies.
    - `rg -n "harmful_expert_indices =|harmful_attn_heads =|num_harmful_experts =" safemoe/interventions/surgery.py` finds the config updates written into the saved checkpoint config.
    - `rg -n "router_source_expert_indices|router_target_expert_indices" safemoe/interventions/surgery.py tests/safemoe/test_checkpoint_surgery.py` returns no matches.
    - `pytest tests/safemoe/test_checkpoint_surgery.py::test_surgery_writes_loadable_checkpoint_directory -x` exits 0.
  </acceptance_criteria>
  <done>Researcher can invoke checkpoint surgery through the existing `python -m safemoe` surface and obtain a loadable staged output with the exact manifest-backed harmful layout.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic value-parity verification and fail-closed finalization</name>
  <files>tests/safemoe/test_checkpoint_surgery.py, safemoe/interventions/verify.py, safemoe/interventions/surgery.py, safemoe/interventions/__init__.py</files>
  <read_first>tests/safemoe/test_checkpoint_surgery.py, safemoe/interventions/surgery.py, safemoe/interventions/manifest.py, safemoe/interventions/planner.py, safemoe/config.py, safemoe/masking.py, safemoe/pretrain.py</read_first>
  <behavior>
    - Test 1: `verify_intervention_output()` reloads the saved checkpoint, recomputes expected mapped expert tensors, derived router columns, and target QKV slices from the base checkpoint plus manifest mappings, seed, and `noise_scale`, and returns `ok=True` only when actual output data matches the manifest-declared mapping semantics and shapes.
    - Test 2: Mutating a target tensor value or swapping one source-to-target mapping while preserving shapes causes `verify_intervention_output()` to raise `ValueError("Checkpoint surgery verification failed")`.
    - Test 3: A failed verification leaves no finalized `checkpoints/<base>-surgery-<manifest_id>/` directory behind; only the temporary staging directory may exist during the failed run and must be removed before exit.
  </behavior>
  <action>Extend `tests/safemoe/test_checkpoint_surgery.py` with `test_verifier_fails_on_manifest_mismatch` that corrupts one target tensor value or intentionally swaps one expert mapping while keeping tensor shapes unchanged. Implement `safemoe/interventions/verify.py` with `verify_intervention_output(base_checkpoint_dir: Path, output_checkpoint_dir: Path, manifest: InterventionManifest) -> dict`. Reload the base and output checkpoints on CPU, rebuild `SafeMoEConfig` from `model_config.yaml`, instantiate `GPT(output_config)`, and call `model.load_state_dict(...)` to prove reloadability. Then recompute the expected mapped expert tensors, derived router columns, and target QKV slices from the untouched base checkpoint using the same mutation order and CPU generator recipe as `apply_intervention_manifest`: layers ascending, then expert pairs in manifest order for `fc_1.weight`, `fc_2.weight`, `proj.weight`, then derived router columns from `derived_router_column_pairs(manifest)`, then attention head pairs in manifest order for Q, K, and V row slices. Use those recomputed values to verify manifest source-to-target mapping semantics for every mutated expert tensor, every derived router column, and every target QKV slice, while also asserting the corresponding shapes are equal; deterministic tensor recomputation is the implementation mechanism, but the required contract remains reload success plus manifest mapping/shape correctness. Assert `output_config.harmful_expert_indices == manifest.target_harmful_expert_indices`, `output_config.harmful_attn_heads == manifest.target_harmful_attn_heads`, and `output_config.num_harmful_experts == len(manifest.target_harmful_expert_indices)`. Write `verification_report.json` with keys `ok`, `reload_ok`, `expert_pairs`, `head_pairs`, `derived_router_column_pairs`, `checks`, and `mismatches`, and write `verification_summary.md` headed by `# Checkpoint Surgery Verification` with a line `Result: PASS` or `Result: FAIL`. In `run_checkpoint_surgery(...)`, call `verify_intervention_output(...)` against the staging directory before finalizing; on any exception, remove the staging directory and re-raise `ValueError("Checkpoint surgery verification failed")`; on success, atomically rename the staging directory to the final `checkpoints/<output_checkpoint_dirname>` path.</action>
  <verify>
    <automated>pytest tests/safemoe/test_checkpoint_surgery.py::test_verifier_fails_on_manifest_mismatch -x</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Checkpoint surgery verification failed" safemoe/interventions/verify.py safemoe/interventions/surgery.py tests/safemoe/test_checkpoint_surgery.py` finds the exact failure contract.
    - `rg -n "# Checkpoint Surgery Verification|Result: PASS|Result: FAIL" safemoe/interventions/verify.py` finds the required human-readable summary format.
    - `rg -n "\"verification_report.json\"|\"verification_summary.md\"" safemoe/interventions/verify.py safemoe/interventions/surgery.py` finds both required report artifact names.
    - `rg -n "derived_router_column_pairs|mismatches|Checkpoint surgery verification failed" safemoe/interventions/verify.py tests/safemoe/test_checkpoint_surgery.py` finds derived router-pair reporting and mismatch handling.
    - `pytest tests/safemoe/test_checkpoint_surgery.py::test_verifier_fails_on_manifest_mismatch -x` exits 0.
  </acceptance_criteria>
  <done>Any invalid Phase 6 surgery artifact fails before final output publication, while valid outputs carry machine-readable and human-readable verification artifacts for downstream use.</done>
</task>

</tasks>

<verification>
Run `pytest tests/safemoe/test_checkpoint_surgery.py tests/safemoe/test_registry.py tests/safemoe/test_ablate.py -x` after the plan completes. Then run one CLI smoke command against a small synthetic checkpoint fixture or a safe local test fixture to confirm `python -m safemoe surgery --help` exposes the new subcommand. A later manual spot-check may run one real manifest-backed surgery against `checkpoints/Qwen3-30B-A3B-Base`.
</verification>

<success_criteria>
`INIT-02` and `INIT-03` are satisfied: a researcher can create a manifest-backed post-surgery Qwen checkpoint that reloads normally, carries the harmful layout in `model_config.yaml`, and produces hard-fail verification reports whenever actual expert tensors, derived router columns, or QKV slices violate the manifest-declared source-to-target mapping semantics or shape expectations.
</success_criteria>

<output>
After completion, create `.planning/phases/06-checkpoint-surgery/06-02-SUMMARY.md`
</output>
