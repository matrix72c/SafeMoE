---
phase: 03-sgtm-training-loop
plan: "05"
type: execute
wave: 2
depends_on: ["03-04"]
files_modified: []
autonomous: false
gap_closure: true
requirements: [TRAIN-01, TRAIN-02, TRAIN-03]

must_haves:
  truths:
    - "Training launches without error on real TinyStories bilingual data using python -m safemoe pretrain"
    - "Per-split loss (loss_D_std, loss_D_harmful, loss_D_unlabeled) is logged during training"
    - "Loss decreases over steps for all three split types, confirming all masking paths allow learning"
  artifacts:
    - path: "safemoe/pretrain.py"
      provides: "Running SGTM training loop that produces a checkpoint and logs per-split loss"
    - path: "safemoe/configs/safemoe-tinystories.yaml"
      provides: "Complete YAML config with upsample_* + harmful_attn_heads + micro_batch_size fields"
  key_links:
    - from: "python -m safemoe pretrain --config safemoe/configs/safemoe-tinystories.yaml"
      to: "safemoe/__main__.py -> pretrain.setup() -> pretrain.main() -> fit()"
      via: "CLI launch producing checkpoint + per-split loss logs"
---

<objective>
Human-verify Success Criterion 3: training loss decreases over steps for all three split types (D_std, D_harmful, D_unlabeled) when running on real TinyStories bilingual data.

Purpose: This is the only gap that cannot be automated — it requires a real multi-step training run with on-disk data. Automated tests confirm structural correctness; this checkpoint confirms behavioral correctness (the model actually learns from all three streams without masking errors stalling optimization).
Output: Confirmed loss convergence across all three splits, or identified regressions in the training loop.
</objective>

<execution_context>
@/home/jincheng1/.claude/get-shit-done/workflows/execute-plan.md
@/home/jincheng1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/03-sgtm-training-loop/03-04-SUMMARY.md
@.planning/phases/03-sgtm-training-loop/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify data preparation and check training prerequisites</name>
  <files></files>
  <action>
    Before the human training run, verify the prepared TinyStories data exists and the CLI entry point is functional.

    Run these checks:
    1. Confirm on-disk data cache exists:
       ```
       ls data/.cache/ 2>/dev/null || echo "No cache dir found"
       ```
       If data is missing, the training run cannot proceed. Note the x/y sweep parameters from safemoe/configs/safemoe-tinystories.yaml and run prepare.py if needed.

    2. Confirm CLI help works (smoke test):
       ```
       python -m safemoe pretrain --help
       ```
       Should exit 0 and show all setup() args including upsample_std, upsample_harmful, upsample_unlabeled.

    3. Print the current YAML config:
       ```
       cat safemoe/configs/safemoe-tinystories.yaml
       ```
       Confirm harmful_attn_heads, upsample_*, micro_batch_size, gradient_accumulation_iters are all present.

    Report findings inline. If data is missing, output the exact prepare.py command the user should run before the training verification. If CLI help fails, report the error.
  </action>
  <verify>
    <automated>cd /mnt/shared-storage-gpfs2/wenxiaoyu-gpfs02/jincheng/safemoe && python -m safemoe pretrain --help 2>&1 | head -20</automated>
  </verify>
  <done>CLI help exits 0. Data cache status reported. User has all information needed for the training run in Task 2.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    SGTM pretraining script (safemoe/pretrain.py) with:
    - Dual AdamW optimizers (theta_harmful and theta_std)
    - 3-path SGTM branching per split label (D_harmful, D_std, D_unlabeled)
    - GradientMasker.mask() called once per D_harmful optimizer step
    - ActivationMasker.enable/disable bracketing D_std micro-batch window
    - Both optimizers stepping on D_unlabeled with no masking
    - Per-split loss logging (loss_D_std, loss_D_harmful, loss_D_unlabeled)
    - CLI entry point: python -m safemoe pretrain
  </what-built>
  <how-to-verify>
    Run a short training session (at least 100 optimizer steps) with the TinyStories bilingual data:

    ```bash
    cd /mnt/shared-storage-gpfs2/wenxiaoyu-gpfs02/jincheng/safemoe

    python -m safemoe pretrain \
      safemoe-tinystories \
      --config safemoe/configs/safemoe-tinystories.yaml \
      --out_dir out/verify-phase3 \
      --train.max_tokens 200000
    ```

    (Adjust --train.max_tokens or add --train.log_interval 10 for more frequent logging.)

    Expected behavior:
    1. Training launches without error
    2. Console output shows per-step loss with "split_label" field cycling through D_std / D_harmful / D_unlabeled
    3. Per-split loss values (loss_D_std, loss_D_harmful, loss_D_unlabeled) appear in logs
    4. After 100+ steps, all three per-split losses trend downward (later-step mean lower than early-step mean)
    5. A checkpoint file is produced at out/verify-phase3/

    If data is not prepared yet, run:
    ```bash
    python -m safemoe data prepare --x 20 --y 25 --tokenizer_name "Qwen/Qwen3-30B-A3B-Base"
    ```
    (adjust x/y and tokenizer_name to match safemoe-tinystories.yaml settings)
  </how-to-verify>
  <resume-signal>
    Type "approved" if all three per-split losses trend downward and a checkpoint was produced.
    Type "failed: [describe what went wrong]" if training errors or loss does not decrease.
    Type "data-missing" if on-disk data is not yet prepared (training cannot run).
  </resume-signal>
</task>

</tasks>

<verification>
After the human verification step is approved:
- Phase 3 Success Criterion 3 is satisfied: training loss decreases over steps for all three split types
- All three verification gaps from VERIFICATION.md are resolved:
  - Gap 1: behavioral tests GREEN (from plan 03-04)
  - Gap 2: REQUIREMENTS.md TRAIN-02 updated (from plan 03-04)
  - Gap 3: loss convergence confirmed (this plan)
</verification>

<success_criteria>
Human confirms:
1. `python -m safemoe pretrain` launches and runs at least 100 optimizer steps without error
2. Per-split loss (loss_D_std, loss_D_harmful, loss_D_unlabeled) is visible in training logs
3. All three per-split losses trend downward over the training run
4. A checkpoint file is produced at the specified out_dir
</success_criteria>

<output>
After completion, create `.planning/phases/03-sgtm-training-loop/03-05-SUMMARY.md`
</output>
