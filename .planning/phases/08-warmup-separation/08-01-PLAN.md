---
phase: 08-warmup-separation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/safemoe/test_warmup_separation.py
  - safemoe/model.py
  - safemoe/pretrain.py
  - safemoe/configs/safemoe-qwen-phase8-warmup.yaml
autonomous: true
requirements:
  - WARM-01
  - WARM-02
must_haves:
  truths:
    - "Researcher can start a dedicated `warmup` stage that trains only on `D_std` and `D_harmful`, while next-token loss stays active."
    - "Warmup logs LM loss, routing loss, total loss, and harmful-routing mass separately for `D_std` and `D_harmful`."
    - "The routing objective increases harmful-expert routing mass for `D_harmful` batches and suppresses it for `D_std` batches through one explicit margin-based contract."
  artifacts:
    - path: "tests/safemoe/test_warmup_separation.py"
      provides: "Phase 8 RED/GREEN coverage for warmup split selection, routing supervision, and loss logging."
      min_lines: 120
    - path: "safemoe/model.py"
      provides: "Differentiable harmful-routing-mass capture from SafeMoE top-k router probabilities."
      exports: ["SafeMoELayer"]
    - path: "safemoe/pretrain.py"
      provides: "Stage-gated warmup execution, split filtering, routing-loss computation, and warmup metric logging."
      exports: ["setup", "fit", "warmup_routing_loss", "active_split_labels"]
    - path: "safemoe/configs/safemoe-qwen-phase8-warmup.yaml"
      provides: "Canonical direct-Qwen warmup config with explicit stage and warmup thresholds."
  key_links:
    - from: "safemoe/model.py"
      to: "safemoe/pretrain.py"
      via: "pretrain reads differentiable harmful-routing mass recorded by each SafeMoELayer after forward()"
      pattern: "_last_harmful_routing_mass|collect_warmup_routing_mass"
    - from: "safemoe/pretrain.py"
      to: "safemoe/data/datamodule.py"
      via: "warmup stage samples only active split labels `D_std` and `D_harmful`"
      pattern: "active_split_labels|random\\.choices"
    - from: "safemoe/pretrain.py"
      to: "safemoe/configs/safemoe-qwen-phase8-warmup.yaml"
      via: "YAML config drives exact warmup fields and enforces `upsample_unlabeled: 0.0`"
      pattern: "stage: warmup|warmup_routing_loss_weight"
---

<objective>
Extend the existing direct-Qwen trainer with a dedicated warmup stage that keeps next-token loss active, excludes `D_unlabeled`, and adds a differentiable routing-separation objective for `D_std` versus `D_harmful`.

Purpose: Close `WARM-01` and `WARM-02` on the same `safemoe/pretrain.py` path already blessed in Phases 5-7.
Output: Warmup-stage tests, stage-gated trainer changes, differentiable routing-mass capture, and a canonical Phase 8 warmup YAML.
</objective>

<execution_context>
@/home/jincheng1/.codex/get-shit-done/workflows/execute-plan.md
@/home/jincheng1/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-warmup-separation/08-CONTEXT.md
@.planning/phases/08-warmup-separation/08-RESEARCH.md
@.planning/phases/08-warmup-separation/08-VALIDATION.md
@.planning/phases/05-environment-runtime-gate/05-02-SUMMARY.md
@.planning/phases/06-checkpoint-surgery/06-02-SUMMARY.md
@.planning/phases/07-registry-and-routing-observability/07-02-SUMMARY.md
@safemoe/model.py
@safemoe/pretrain.py
@safemoe/data/datamodule.py
@safemoe/configs/safemoe-qwen-phase5-gate.yaml
@tests/safemoe/test_pretrain.py

<interfaces>
From `safemoe/pretrain.py`:
```python
SPLIT_LABELS = ["D_std", "D_harmful", "D_unlabeled"]

def setup(..., upsample_std: Optional[float], upsample_harmful: Optional[float], upsample_unlabeled: Optional[float]):
    ...

def fit(..., upsample_std: float = 1.0, upsample_harmful: float = 1.0, upsample_unlabeled: float = 1.0, ...):
    ...
```

From `safemoe/model.py`:
```python
class SafeMoELayer(LLaMAMoE):
    def forward(self, x: torch.Tensor) -> torch.Tensor:
        ...
        self._last_indices = indices
        probs = probs.softmax(dim=1, dtype=torch.float).to(dtype=x_flat.dtype)
```

Pinned Phase 8 metric contract:
```text
loss_lm_D_std
loss_routing_D_std
loss_total_D_std
routing_harmful_mass_D_std
loss_lm_D_harmful
loss_routing_D_harmful
loss_total_D_harmful
routing_harmful_mass_D_harmful
```

Pinned warmup config values:
```text
stage: warmup
upsample_std: 1.0
upsample_harmful: 1.0
upsample_unlabeled: 0.0
warmup_routing_loss_weight: 0.1
warmup_harmful_mass_floor: 0.6
warmup_std_mass_ceiling: 0.4
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED coverage for warmup-stage split filtering and routing-loss logging</name>
  <files>tests/safemoe/test_warmup_separation.py</files>
  <read_first>tests/safemoe/test_pretrain.py, tests/safemoe/test_evaluate.py, safemoe/pretrain.py, safemoe/model.py, safemoe/data/datamodule.py, .planning/phases/08-warmup-separation/08-CONTEXT.md, .planning/phases/08-warmup-separation/08-VALIDATION.md</read_first>
  <behavior>
    - Test 1: A `stage="warmup"` fit run samples only `D_std` and `D_harmful`, never emits `loss_*_D_unlabeled`, and logs `loss_lm_*`, `loss_routing_*`, `loss_total_*`, and `routing_harmful_mass_*` for the observed labeled split.
    - Test 2: `warmup_routing_loss(harmful_mass, "D_harmful", harmful_mass_floor=0.6, std_mass_ceiling=0.4)` is strictly positive when `harmful_mass < 0.6`, while `warmup_routing_loss(harmful_mass, "D_std", ...)` is strictly positive when `harmful_mass > 0.4`.
    - Test 3: `setup()` or `fit()` rejects any warmup run with `upsample_unlabeled != 0.0` by raising `ValueError`.
  </behavior>
  <action>Create `tests/safemoe/test_warmup_separation.py` with a tiny in-memory warmup fixture modeled on `tests/safemoe/test_pretrain.py`. Add `test_warmup_stage_logs_separate_losses_and_excludes_unlabeled`, `test_warmup_routing_supervision_penalizes_wrong_direction`, and `test_warmup_rejects_nonzero_unlabeled_weight`. In the first test, force one-step warmup execution and assert the exact logged keys `loss_lm_D_std`, `loss_routing_D_std`, `loss_total_D_std`, `routing_harmful_mass_D_std` or their `D_harmful` counterparts appear, while `loss_lm_D_unlabeled`, `loss_routing_D_unlabeled`, `loss_total_D_unlabeled`, and `routing_harmful_mass_D_unlabeled` do not appear anywhere in the logged metrics. In the second test, assert concrete numeric inequalities around the exact thresholds `0.6` and `0.4`. In the third test, pass `stage="warmup"` plus `upsample_unlabeled=0.1` and assert `ValueError` mentions `upsample_unlabeled must be 0.0 for warmup`.</action>
  <verify>
    <automated>pytest tests/safemoe/test_warmup_separation.py -q -k "warmup_loss_logging or routing_supervision or rejects_nonzero_unlabeled"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "loss_lm_D_std|loss_routing_D_std|loss_total_D_std|routing_harmful_mass_D_std|loss_lm_D_harmful|loss_routing_D_harmful|loss_total_D_harmful|routing_harmful_mass_D_harmful" tests/safemoe/test_warmup_separation.py` finds the exact warmup metric keys in the test file.
    - `rg -n "loss_lm_D_unlabeled|loss_routing_D_unlabeled|loss_total_D_unlabeled|routing_harmful_mass_D_unlabeled" tests/safemoe/test_warmup_separation.py` finds explicit omission assertions for unlabeled warmup metrics.
    - `rg -n "0\\.6|0\\.4|upsample_unlabeled must be 0\\.0 for warmup" tests/safemoe/test_warmup_separation.py` finds the exact thresholds and error string in the tests.
    - `pytest tests/safemoe/test_warmup_separation.py -q -k "warmup_loss_logging or routing_supervision or rejects_nonzero_unlabeled"` exits 0.
  </acceptance_criteria>
  <done>Phase 8 has locked tests for the warmup split set, routing-loss directionality, and the separate LM versus routing logging contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement stage-gated warmup routing supervision and the canonical warmup config</name>
  <files>tests/safemoe/test_warmup_separation.py, safemoe/model.py, safemoe/pretrain.py, safemoe/configs/safemoe-qwen-phase8-warmup.yaml</files>
  <read_first>tests/safemoe/test_warmup_separation.py, safemoe/model.py, safemoe/pretrain.py, safemoe/configs/safemoe-qwen-phase5-gate.yaml, safemoe/data/datamodule.py, .planning/phases/08-warmup-separation/08-RESEARCH.md</read_first>
  <behavior>
    - Test 1: `SafeMoELayer.forward()` records differentiable harmful-routing mass from top-k probabilities so the trainer can backpropagate through the warmup routing objective.
    - Test 2: `fit(..., stage="warmup", warmup_routing_loss_weight=0.1, warmup_harmful_mass_floor=0.6, warmup_std_mass_ceiling=0.4)` trains on `D_std` and `D_harmful` only and logs separate LM, routing, and total losses.
    - Test 3: The repo contains one canonical warmup YAML at `safemoe/configs/safemoe-qwen-phase8-warmup.yaml` with `stage: warmup`, `upsample_unlabeled: 0.0`, and the exact warmup thresholds/weights used above.
  </behavior>
  <action>In `safemoe/model.py`, after top-k routing probabilities are computed, store a differentiable scalar on each layer named `_last_harmful_routing_mass` equal to the mean harmful-expert probability mass across all routed tokens in that layer. Compute it from the actual top-k probabilities and `indices`, not from detached dispatch counts: build a boolean mask with the current `harmful_expert_indices`, multiply it by the top-k probabilities, sum across the selected experts, and average across tokens. In `safemoe/pretrain.py`, add explicit stage-gating fields to `setup()`, `main()`, and `fit()`: `stage: Literal["transfer", "warmup"] = "transfer"`, `warmup_routing_loss_weight: float = 0.1`, `warmup_harmful_mass_floor: float = 0.6`, and `warmup_std_mass_ceiling: float = 0.4`. Add `active_split_labels(stage)` returning `["D_std", "D_harmful"]` for warmup and the existing three-way list for transfer. When `stage == "warmup"`, raise `ValueError("upsample_unlabeled must be 0.0 for warmup")` unless the unlabeled weight is exactly `0.0`; build the split iterator map and weighted `random.choices(...)` list from the active labels only, so no unlabeled batch can be sampled. Add `warmup_routing_loss(harmful_mass, split_label, harmful_mass_floor=0.6, std_mass_ceiling=0.4)` using `torch.nn.functional.softplus`: for `D_harmful`, return `softplus(harmful_mass_floor - harmful_mass)`; for `D_std`, return `softplus(harmful_mass - std_mass_ceiling)`; for any other split, return a zero scalar on the same device/dtype. During the training step, keep next-token loss unchanged as `lm_loss`, collect the mean of every `SafeMoELayer._last_harmful_routing_mass` value into one scalar `harmful_mass`, compute `routing_loss`, set `total_loss = lm_loss + warmup_routing_loss_weight * routing_loss` in warmup mode, and backpropagate `total_loss / accum_iters`. Log the exact keys `loss_lm_<split>`, `loss_routing_<split>`, `loss_total_<split>`, and `routing_harmful_mass_<split>` for warmup steps while preserving the generic top-level `loss` metric as the total loss. Create `safemoe/configs/safemoe-qwen-phase8-warmup.yaml` by copying the Phase 5 direct-Qwen style and setting: `model_name: Qwen3-30B-A3B-Base`, `tokenizer_dir: checkpoints/Qwen3-30B-A3B-Base`, `devices: 4`, `num_nodes: 1`, `seed: 42`, `logger_name: tensorboard`, `stage: warmup`, `warmup_routing_loss_weight: 0.1`, `warmup_harmful_mass_floor: 0.6`, `warmup_std_mass_ceiling: 0.4`, `train.max_seq_length: 1024`, `upsample_std: 1.0`, `upsample_harmful: 1.0`, and `upsample_unlabeled: 0.0`.</action>
  <verify>
    <automated>pytest tests/safemoe/test_warmup_separation.py -q -k "warmup_loss_logging or routing_supervision or rejects_nonzero_unlabeled" && pytest tests/safemoe/test_pretrain.py -q -k "produces_checkpoint or routing parity"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "_last_harmful_routing_mass" safemoe/model.py safemoe/pretrain.py` finds the differentiable routing-mass handoff between model and trainer.
    - `rg -n "stage: Literal\\[\"transfer\", \"warmup\"\\]|def active_split_labels|def warmup_routing_loss|upsample_unlabeled must be 0\\.0 for warmup" safemoe/pretrain.py` finds the exact stage and warmup helper contracts.
    - `rg -n "loss_lm_|loss_routing_|loss_total_|routing_harmful_mass_" safemoe/pretrain.py tests/safemoe/test_warmup_separation.py` finds the full warmup logging schema.
    - `rg -n "stage: warmup|warmup_routing_loss_weight: 0\\.1|warmup_harmful_mass_floor: 0\\.6|warmup_std_mass_ceiling: 0\\.4|upsample_unlabeled: 0\\.0" safemoe/configs/safemoe-qwen-phase8-warmup.yaml` finds the exact canonical warmup config values.
    - `pytest tests/safemoe/test_warmup_separation.py -q -k "warmup_loss_logging or routing_supervision or rejects_nonzero_unlabeled"` exits 0.
  </acceptance_criteria>
  <done>The direct-Qwen trainer can run a dedicated labeled-only warmup stage with measurable routing supervision and a reproducible YAML entrypoint.</done>
</task>

</tasks>

<verification>
Run `pytest tests/safemoe/test_warmup_separation.py -q` after the plan completes, then run `pytest tests/safemoe/test_pretrain.py tests/safemoe/test_warmup_separation.py -q` to confirm the new warmup path does not regress the existing trainer contracts. Inspect one warmup log stream and confirm only `D_std` / `D_harmful` warmup metrics appear.
</verification>

<success_criteria>
`WARM-01` and `WARM-02` are satisfied: researchers can launch a dedicated warmup stage on only `D_std` and `D_harmful`, keep next-token loss active, observe separate routing versus LM losses, and train against an explicit aggregate harmful-routing-mass objective that pushes harmful batches up and standard batches down.
</success_criteria>

<output>
After completion, create `.planning/phases/08-warmup-separation/08-01-SUMMARY.md`
</output>
