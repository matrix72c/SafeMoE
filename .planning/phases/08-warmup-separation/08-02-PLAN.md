---
phase: 08-warmup-separation
plan: "02"
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - tests/safemoe/test_warmup_separation.py
  - safemoe/evaluate.py
  - safemoe/pretrain.py
autonomous: true
requirements:
  - WARM-03
must_haves:
  truths:
    - "Researcher can compare the same intervention lineage before warmup and after the final warmup checkpoint using one report."
    - "The final warmup report passes only when routing separation improves and `D_std` perplexity stays within a fixed non-regression bound."
    - "Phase 9 receives exactly one blessed warmup handoff checkpoint, and that checkpoint exists only when the final warmup acceptance check passes."
  artifacts:
    - path: "tests/safemoe/test_warmup_separation.py"
      provides: "Coverage for same-lineage warmup acceptance reporting and checkpoint blessing."
      min_lines: 180
    - path: "safemoe/evaluate.py"
      provides: "Warmup acceptance helper that writes pre/post routing and perplexity comparisons."
      exports: ["evaluate_warmup_acceptance"]
    - path: "safemoe/pretrain.py"
      provides: "Final warmup acceptance gate and blessed checkpoint publication."
      exports: ["main", "save_checkpoint"]
  key_links:
    - from: "safemoe/pretrain.py"
      to: "safemoe/evaluate.py"
      via: "final warmup stage compares `initial_checkpoint_dir` against `out_dir/final` through one acceptance helper"
      pattern: "evaluate_warmup_acceptance"
    - from: "safemoe/evaluate.py"
      to: "safemoe/observability.py"
      via: "report reuses the Phase 7 routing artifact contract for both pre and post checkpoints"
      pattern: "routing_attribution|routing_observability"
    - from: "safemoe/pretrain.py"
      to: "out_dir/warmup-blessed"
      via: "final pass copies the accepted final checkpoint into the dedicated Phase 9 handoff directory"
      pattern: "warmup-blessed|copytree"
---

<objective>
Add the confound-controlled Phase 8 acceptance report and blessed handoff checkpoint that compare the pre-warmup surgery checkpoint against the final warmup checkpoint on the same evaluation path.

Purpose: Close `WARM-03` with a pass/fail artifact and one unambiguous Phase 9 resume target.
Output: Warmup acceptance tests, a same-lineage pre/post report writer, and a final blessed-checkpoint gate in the warmup training flow.
</objective>

<execution_context>
@/home/jincheng1/.codex/get-shit-done/workflows/execute-plan.md
@/home/jincheng1/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-warmup-separation/08-CONTEXT.md
@.planning/phases/08-warmup-separation/08-RESEARCH.md
@.planning/phases/08-warmup-separation/08-VALIDATION.md
@.planning/phases/08-warmup-separation/08-01-PLAN.md
@.planning/phases/04-ablation-evaluation/04-05-SUMMARY.md
@.planning/phases/07-registry-and-routing-observability/07-02-SUMMARY.md
@safemoe/evaluate.py
@safemoe/pretrain.py
@tests/safemoe/test_evaluate.py
@tests/safemoe/test_warmup_separation.py

<interfaces>
From `safemoe/evaluate.py`:
```python
def evaluate_perplexity(original_ckpt_dir: Path, ablated_ckpt_dir: Optional[Path] = None, data_mock=None, out_dir: Optional[Path] = None) -> dict: ...
def routing_attribution(ckpt_dir: Path, data_mock=None) -> dict: ...
```

Pinned Phase 8 acceptance thresholds:
```text
std_ppl_regression_tolerance = 0.05
required_post_routing_margin = 0.10
required_margin_gain = 0.00
blessed_checkpoint_name = warmup-blessed
```

Pinned artifact names:
```text
warmup_acceptance.json
warmup_acceptance.md
warmup-blessed/
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED coverage for the same-lineage warmup acceptance report</name>
  <files>tests/safemoe/test_warmup_separation.py</files>
  <read_first>tests/safemoe/test_warmup_separation.py, tests/safemoe/test_evaluate.py, safemoe/evaluate.py, safemoe/pretrain.py, .planning/phases/08-warmup-separation/08-CONTEXT.md, .planning/phases/08-warmup-separation/08-VALIDATION.md</read_first>
  <behavior>
    - Test 1: `evaluate_warmup_acceptance(pre_ckpt_dir, post_ckpt_dir, ...)` writes `warmup_acceptance.json` and `warmup_acceptance.md`, includes pre/post routing and perplexity sections, and marks pass when the post-warmup routing margin is at least `0.10`, improves over pre-warmup, and `D_std` perplexity increases by no more than `5%`.
    - Test 2: If `D_std` perplexity regression exceeds `5%`, the report marks `pass: false` even when routing separation improved.
    - Test 3: A failed warmup acceptance check does not create `warmup-blessed`, while a passing check does create `warmup-blessed/lit_model.pth`.
  </behavior>
  <action>Extend `tests/safemoe/test_warmup_separation.py` with `test_warmup_acceptance_report_passes_on_same_lineage_improvement`, `test_warmup_acceptance_report_fails_on_std_regression`, and `test_warmup_blessed_checkpoint_requires_pass`. Use synthetic pre/post metric fixtures so the tests assert exact thresholds: `required_post_routing_margin=0.10`, `required_margin_gain=0.00`, and `std_ppl_regression_tolerance=0.05`. Assert the JSON artifact contains keys `pre`, `post`, `delta`, `criteria`, `routing_margin_pre`, `routing_margin_post`, `routing_margin_gain`, `std_ppl_ratio`, `pass`, and `blessed_checkpoint_dir`. Assert the Markdown artifact starts with `# Warmup Acceptance Report` and includes a line `Result: PASS` or `Result: FAIL`.</action>
  <verify>
    <automated>pytest tests/safemoe/test_warmup_separation.py -q -k "warmup_acceptance_report or warmup_blessed_checkpoint"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "warmup_acceptance.json|warmup_acceptance.md|warmup-blessed|Result: PASS|Result: FAIL" tests/safemoe/test_warmup_separation.py` finds the exact artifact and report strings in the test file.
    - `rg -n "0\\.10|0\\.05|routing_margin_post|std_ppl_ratio|blessed_checkpoint_dir" tests/safemoe/test_warmup_separation.py` finds the exact thresholds and report keys in the tests.
    - `pytest tests/safemoe/test_warmup_separation.py -q -k "warmup_acceptance_report or warmup_blessed_checkpoint"` exits 0.
  </acceptance_criteria>
  <done>The acceptance and blessing contract is locked in tests before the Phase 8 report logic is implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the final warmup acceptance gate and blessed checkpoint publication</name>
  <files>tests/safemoe/test_warmup_separation.py, safemoe/evaluate.py, safemoe/pretrain.py</files>
  <read_first>tests/safemoe/test_warmup_separation.py, safemoe/evaluate.py, safemoe/pretrain.py, tests/safemoe/test_evaluate.py, .planning/phases/08-warmup-separation/08-01-PLAN.md, .planning/phases/04-ablation-evaluation/04-05-SUMMARY.md</read_first>
  <behavior>
    - Test 1: `evaluate_warmup_acceptance()` compares the pre-warmup checkpoint and final warmup checkpoint through the same `evaluate_perplexity()` and `routing_attribution()` paths used elsewhere in the repo, then writes one pass/fail report.
    - Test 2: The final warmup training flow calls the acceptance helper automatically after saving `out_dir/final/lit_model.pth`, and raises a hard failure if the report does not pass.
    - Test 3: A passing warmup report copies the accepted final checkpoint directory to `out_dir/warmup-blessed/` and records that directory in the report as the only blessed Phase 9 handoff target.
  </behavior>
  <action>In `safemoe/evaluate.py`, add `evaluate_warmup_acceptance(pre_ckpt_dir: Path, post_ckpt_dir: Path, data_mock=None, out_dir: Optional[Path] = None, std_ppl_regression_tolerance: float = 0.05, required_post_routing_margin: float = 0.10, required_margin_gain: float = 0.00, blessed_checkpoint_name: str = "warmup-blessed") -> dict`. The helper must call `evaluate_perplexity(..., ablated_ckpt_dir=None)` and `routing_attribution(...)` for both checkpoints, compute `routing_margin_pre = pre.routing_harmful_frac_D_harmful - pre.routing_harmful_frac_D_std`, `routing_margin_post = post.routing_harmful_frac_D_harmful - post.routing_harmful_frac_D_std`, `routing_margin_gain = routing_margin_post - routing_margin_pre`, and `std_ppl_ratio = post.val_ppl_D_std / pre.val_ppl_D_std`. Mark the report as pass only if `routing_margin_post >= 0.10`, `routing_margin_gain > 0.00`, and `std_ppl_ratio <= 1.05`. Write `warmup_acceptance.json` with keys `pre`, `post`, `delta`, `criteria`, `routing_margin_pre`, `routing_margin_post`, `routing_margin_gain`, `std_ppl_ratio`, `pass`, and `blessed_checkpoint_dir`. Write `warmup_acceptance.md` headed `# Warmup Acceptance Report` with exact lines for `Result: PASS` or `Result: FAIL`, the numeric threshold summary, and the pre/post checkpoint paths. In `safemoe/pretrain.py`, add final warmup gating after `save_checkpoint(..., out_dir / "final" / "lit_model.pth")`: when `stage == "warmup"`, require `initial_checkpoint_dir` to be present, call `evaluate_warmup_acceptance(initial_checkpoint_dir, out_dir / "final", out_dir=out_dir, std_ppl_regression_tolerance=0.05, required_post_routing_margin=0.10, required_margin_gain=0.00, blessed_checkpoint_name="warmup-blessed")`, and if the returned report has `pass == false`, raise `ValueError("Warmup acceptance failed")` after writing the report. If it passes, copy the entire `out_dir / "final"` checkpoint directory to `out_dir / "warmup-blessed"` using filesystem copy semantics that preserve `lit_model.pth`, `model_config.yaml`, tokenizer files, and `hyperparameters.yaml`; do not pick an earlier checkpoint or best intermediate step. Record the blessed directory path in both the returned report and the Markdown summary.</action>
  <verify>
    <automated>pytest tests/safemoe/test_warmup_separation.py -q -k "warmup_acceptance_report or warmup_blessed_checkpoint" && pytest tests/safemoe/test_evaluate.py -q -k "evaluate_perplexity or routing_observability"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "def evaluate_warmup_acceptance|warmup_acceptance.json|warmup_acceptance.md|Warmup acceptance failed" safemoe/evaluate.py safemoe/pretrain.py` finds the exact helper, artifact names, and hard-failure string.
    - `rg -n "required_post_routing_margin:|required_margin_gain:|std_ppl_regression_tolerance:" safemoe/evaluate.py` finds the exact acceptance thresholds in code.
    - `rg -n "routing_margin_pre|routing_margin_post|routing_margin_gain|std_ppl_ratio|blessed_checkpoint_dir" safemoe/evaluate.py tests/safemoe/test_warmup_separation.py` finds the exact report schema.
    - `rg -n "warmup-blessed|copytree|ValueError\\(\"Warmup acceptance failed\"\\)" safemoe/pretrain.py` finds the dedicated blessed checkpoint path and fail-closed behavior.
    - `pytest tests/safemoe/test_warmup_separation.py -q -k "warmup_acceptance_report or warmup_blessed_checkpoint"` exits 0.
  </acceptance_criteria>
  <done>The final warmup run now produces one same-lineage pass/fail report and exactly one blessed Phase 9 handoff checkpoint when the acceptance bar is satisfied.</done>
</task>

</tasks>

<verification>
Run `pytest tests/safemoe/test_warmup_separation.py -q` and `pytest tests/safemoe/test_evaluate.py tests/safemoe/test_warmup_separation.py -q` after the plan completes. For one warmup run, inspect `warmup_acceptance.json` and `warmup_acceptance.md` in the run directory and confirm `warmup-blessed/` exists only on PASS.
</verification>

<success_criteria>
`WARM-03` is satisfied: researchers can review one confound-controlled pre/post report on the same intervention lineage, see that post-warmup harmful routing separates from standard routing without material `D_std` degradation, and resume Phase 9 from exactly one blessed warmup checkpoint.
</success_criteria>

<output>
After completion, create `.planning/phases/08-warmup-separation/08-02-SUMMARY.md`
</output>
